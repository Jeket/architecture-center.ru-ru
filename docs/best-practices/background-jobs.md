---
title: Руководство по фоновым заданиям
titleSuffix: Best practices for cloud applications
description: Руководство по использованию фоновых задач, выполняемых независимо от пользовательского интерфейса.
author: dragon119
ms.date: 11/05/2018
ms.topic: best-practice
ms.service: architecture-center
ms.subservice: cloud-fundamentals
ms.custom: seodec18
ms.openlocfilehash: 4b96c19dd8613a941a7408e1b99945d5fa0f5364
ms.sourcegitcommit: 1b50810208354577b00e89e5c031b774b02736e2
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/23/2019
ms.locfileid: "54482074"
---
# <a name="background-jobs"></a><span data-ttu-id="ac66d-103">Фоновые задания</span><span class="sxs-lookup"><span data-stu-id="ac66d-103">Background jobs</span></span>

<span data-ttu-id="ac66d-104">Многим типам приложений требуются фоновые задачи, которые выполняются независимо от пользовательского интерфейса.</span><span class="sxs-lookup"><span data-stu-id="ac66d-104">Many types of applications require background tasks that run independently of the user interface (UI).</span></span> <span data-ttu-id="ac66d-105">К примерам относятся пакетные задания, ресурсоемкие задачи обработки и длительные процессы, например рабочие процессы.</span><span class="sxs-lookup"><span data-stu-id="ac66d-105">Examples include batch jobs, intensive processing tasks, and long-running processes such as workflows.</span></span> <span data-ttu-id="ac66d-106">Фоновые задания могут выполняться без вмешательства пользователя. Приложение может запустить задание, а затем продолжить обработку интерактивных запросов пользователей.</span><span class="sxs-lookup"><span data-stu-id="ac66d-106">Background jobs can be executed without requiring user interaction--the application can start the job and then continue to process interactive requests from users.</span></span> <span data-ttu-id="ac66d-107">Это поможет свести к минимуму нагрузку на пользовательский интерфейс приложения, что может увеличить доступность и сократить время интерактивного отклика приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-107">This can help to minimize the load on the application UI, which can improve availability and reduce interactive response times.</span></span>

<span data-ttu-id="ac66d-108">Например, если приложению требуется создавать эскизы рисунков, передаваемых пользователями, оно может делать это как фоновое задание, после чего сохранять эскизы в хранилище, не вынуждая пользователя ожидать, пока процесс будет завершен.</span><span class="sxs-lookup"><span data-stu-id="ac66d-108">For example, if an application is required to generate thumbnails of images that are uploaded by users, it can do this as a background job and save the thumbnail to storage when it is complete--without the user needing to wait for the process to be completed.</span></span> <span data-ttu-id="ac66d-109">Точно так же пользователь, размещающий заказ, может запустить фоновый рабочий процесс, который обрабатывает заказ, а с помощью пользовательского интерфейса пользователь может продолжить просмотр веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-109">In the same way, a user placing an order can initiate a background workflow that processes the order, while the UI allows the user to continue browsing the web app.</span></span> <span data-ttu-id="ac66d-110">По завершении фонового задания оно может обновить хранимые данные заказов и отправить пользователю электронное сообщение, подтверждающее заказ.</span><span class="sxs-lookup"><span data-stu-id="ac66d-110">When the background job is complete, it can update the stored orders data and send an email to the user that confirms the order.</span></span>

<span data-ttu-id="ac66d-111">При выборе реализации в виде задачи или фонового задания основной критерий — может ли задача выполняться без взаимодействия с пользователем и должен ли пользовательский интерфейс ожидать завершения задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-111">When you consider whether to implement a task as a background job, the main criteria is whether the task can run without user interaction and without the UI needing to wait for the job to be completed.</span></span> <span data-ttu-id="ac66d-112">Задачи, требующие, чтобы пользователь или пользовательский интерфейс ожидал, пока они выполняются, не подходят для фоновых заданий.</span><span class="sxs-lookup"><span data-stu-id="ac66d-112">Tasks that require the user or the UI to wait while they are completed might not be appropriate as background jobs.</span></span>

## <a name="types-of-background-jobs"></a><span data-ttu-id="ac66d-113">Типы фоновых заданий</span><span class="sxs-lookup"><span data-stu-id="ac66d-113">Types of background jobs</span></span>

<span data-ttu-id="ac66d-114">Фоновые задания обычно включают одно или несколько заданий следующих типов:</span><span class="sxs-lookup"><span data-stu-id="ac66d-114">Background jobs typically include one or more of the following types of jobs:</span></span>

- <span data-ttu-id="ac66d-115">Задания, интенсивно использующие ЦП, например математические вычисления или анализ структурных моделей.</span><span class="sxs-lookup"><span data-stu-id="ac66d-115">CPU-intensive jobs, such as mathematical calculations or structural model analysis.</span></span>
- <span data-ttu-id="ac66d-116">Задания с большим объемом операций ввода-вывода, например, выполняющие циклы транзакций с хранилищем или индексирование файлов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-116">I/O-intensive jobs, such as executing a series of storage transactions or indexing files.</span></span>
- <span data-ttu-id="ac66d-117">Пакетные задания, например обновление данных каждую ночь или обработка по расписанию.</span><span class="sxs-lookup"><span data-stu-id="ac66d-117">Batch jobs, such as nightly data updates or scheduled processing.</span></span>
- <span data-ttu-id="ac66d-118">Продолжительные рабочие процессы, например выполнение заказов или подготовка служб и систем.</span><span class="sxs-lookup"><span data-stu-id="ac66d-118">Long-running workflows, such as order fulfillment, or provisioning services and systems.</span></span>
- <span data-ttu-id="ac66d-119">Обработка конфиденциальных данных, при которой задача для обработки передается в более безопасное место.</span><span class="sxs-lookup"><span data-stu-id="ac66d-119">Sensitive-data processing where the task is handed off to a more secure location for processing.</span></span> <span data-ttu-id="ac66d-120">Например, вы можете не желать обрабатывать конфиденциальные данные в веб-приложении,</span><span class="sxs-lookup"><span data-stu-id="ac66d-120">For example, you might not want to process sensitive data within a web app.</span></span> <span data-ttu-id="ac66d-121">а вместо этого использовать шаблон, например [шаблон привратника](../patterns/gatekeeper.md), чтобы передавать данные в изолированный фоновый процесс, имеющий доступ к защищенному хранилищу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-121">Instead, you might use a pattern such as the [Gatekeeper pattern](../patterns/gatekeeper.md) to transfer the data to an isolated background process that has access to protected storage.</span></span>

## <a name="triggers"></a><span data-ttu-id="ac66d-122">триггеры;</span><span class="sxs-lookup"><span data-stu-id="ac66d-122">Triggers</span></span>

<span data-ttu-id="ac66d-123">Фоновые задания могут запускаться несколькими различными способами.</span><span class="sxs-lookup"><span data-stu-id="ac66d-123">Background jobs can be initiated in several different ways.</span></span> <span data-ttu-id="ac66d-124">Каждый из них можно отнести к одной из следующих категорий:</span><span class="sxs-lookup"><span data-stu-id="ac66d-124">They fall into one of the following categories:</span></span>

- <span data-ttu-id="ac66d-125">[**Запуск при определенном событии**](#event-driven-triggers).</span><span class="sxs-lookup"><span data-stu-id="ac66d-125">[**Event-driven triggers**](#event-driven-triggers).</span></span> <span data-ttu-id="ac66d-126">Задача запускается в ответ на событие — обычно действие, выполненное пользователем, или шаг в рабочем процессе.</span><span class="sxs-lookup"><span data-stu-id="ac66d-126">The task is started in response to an event, typically an action taken by a user or a step in a workflow.</span></span>
- <span data-ttu-id="ac66d-127">[**Триггеры, управляемые расписанием**](#schedule-driven-triggers).</span><span class="sxs-lookup"><span data-stu-id="ac66d-127">[**Schedule-driven triggers**](#schedule-driven-triggers).</span></span> <span data-ttu-id="ac66d-128">Задача вызывается по расписанию на основе таймера.</span><span class="sxs-lookup"><span data-stu-id="ac66d-128">The task is invoked on a schedule based on a timer.</span></span> <span data-ttu-id="ac66d-129">Это может быть повторяющееся расписание или одноразовый вызов, указанный на более позднее время.</span><span class="sxs-lookup"><span data-stu-id="ac66d-129">This might be a recurring schedule or a one-off invocation that is specified for a later time.</span></span>

### <a name="event-driven-triggers"></a><span data-ttu-id="ac66d-130">Запуск при определенном событии</span><span class="sxs-lookup"><span data-stu-id="ac66d-130">Event-driven triggers</span></span>

<span data-ttu-id="ac66d-131">При управляемом событиями вызове для запуска фоновой задачи используется триггер.</span><span class="sxs-lookup"><span data-stu-id="ac66d-131">Event-driven invocation uses a trigger to start the background task.</span></span> <span data-ttu-id="ac66d-132">Примеры использования триггеров, управляемых событиями:</span><span class="sxs-lookup"><span data-stu-id="ac66d-132">Examples of using event-driven triggers include:</span></span>

- <span data-ttu-id="ac66d-133">Пользовательский интерфейс или другое задание помещает сообщение в очередь.</span><span class="sxs-lookup"><span data-stu-id="ac66d-133">The UI or another job places a message in a queue.</span></span> <span data-ttu-id="ac66d-134">Сообщение содержит данные о действии, которое было выполнено, например: пользователь разместил заказ.</span><span class="sxs-lookup"><span data-stu-id="ac66d-134">The message contains data about an action that has taken place, such as the user placing an order.</span></span> <span data-ttu-id="ac66d-135">Фоновая задача прослушивает эту очередь и обнаруживает поступление нового сообщения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-135">The background task listens on this queue and detects the arrival of a new message.</span></span> <span data-ttu-id="ac66d-136">Она считывает сообщение и использует его в качестве входных данных для фонового задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-136">It reads the message and uses the data in it as the input to the background job.</span></span>
- <span data-ttu-id="ac66d-137">Пользовательский интерфейс или другое задание сохраняет или обновляет значение в хранилище.</span><span class="sxs-lookup"><span data-stu-id="ac66d-137">The UI or another job saves or updates a value in storage.</span></span> <span data-ttu-id="ac66d-138">Фоновая задача отслеживает хранилище и обнаруживает изменения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-138">The background task monitors the storage and detects changes.</span></span> <span data-ttu-id="ac66d-139">Она считывает данные и использует их в качестве входных данных для фонового задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-139">It reads the data and uses it as the input to the background job.</span></span>
- <span data-ttu-id="ac66d-140">Пользовательский интерфейс или другое задание выполняет запрос к конечной точке (по универсальному коду ресурса (URI) HTTPS) или API, предоставляемому как веб-служба.</span><span class="sxs-lookup"><span data-stu-id="ac66d-140">The UI or another job makes a request to an endpoint, such as an HTTPS URI, or an API that is exposed as a web service.</span></span> <span data-ttu-id="ac66d-141">В запросе передаются данные, необходимые для выполнения фоновой задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-141">It passes the data that is required to complete the background task as part of the request.</span></span> <span data-ttu-id="ac66d-142">Конечная точка или веб-служба вызывает фоновую задачу, которая использует эти данные в качестве входных.</span><span class="sxs-lookup"><span data-stu-id="ac66d-142">The endpoint or web service invokes the background task, which uses the data as its input.</span></span>

<span data-ttu-id="ac66d-143">Типичные примеры задач, подходящих для управляемого событиями вызовами: обработка изображений, рабочие процессы, отправка информации в удаленные службы, отправка электронных сообщений и подготовка новых пользователей в многопользовательских приложениях.</span><span class="sxs-lookup"><span data-stu-id="ac66d-143">Typical examples of tasks that are suited to event-driven invocation include image processing, workflows, sending information to remote services, sending email messages, and provisioning new users in multitenant applications.</span></span>

### <a name="schedule-driven-triggers"></a><span data-ttu-id="ac66d-144">Триггеры, управляемые расписанием</span><span class="sxs-lookup"><span data-stu-id="ac66d-144">Schedule-driven triggers</span></span>

<span data-ttu-id="ac66d-145">При управляемом расписанием вызове для запуска фоновой задачи используется таймер.</span><span class="sxs-lookup"><span data-stu-id="ac66d-145">Schedule-driven invocation uses a timer to start the background task.</span></span> <span data-ttu-id="ac66d-146">Примеры использования управляемых расписанием триггеров:</span><span class="sxs-lookup"><span data-stu-id="ac66d-146">Examples of using schedule-driven triggers include:</span></span>

- <span data-ttu-id="ac66d-147">Таймер, выполняемый локально в приложении или в операционной системе, вызывает фоновую задачу на регулярной основе.</span><span class="sxs-lookup"><span data-stu-id="ac66d-147">A timer that is running locally within the application or as part of the application's operating system invokes a background task on a regular basis.</span></span>
- <span data-ttu-id="ac66d-148">Таймер, выполняемый в другом приложении, или служба таймера, например планировщик Azure, отправляет запрос к API или веб-службе на регулярной основе.</span><span class="sxs-lookup"><span data-stu-id="ac66d-148">A timer that is running in a different application, or a timer service such as Azure Scheduler, sends a request to an API or web service on a regular basis.</span></span> <span data-ttu-id="ac66d-149">API или веб-служба вызывает фоновую задачу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-149">The API or web service invokes the background task.</span></span>
- <span data-ttu-id="ac66d-150">Отдельный процесс или приложение запускает таймер, который вызывает фоновую задачу по истечении указанного времени задержки или в определенное время.</span><span class="sxs-lookup"><span data-stu-id="ac66d-150">A separate process or application starts a timer that causes the background task to be invoked once after a specified time delay, or at a specific time.</span></span>

<span data-ttu-id="ac66d-151">Типичные примеры задач, подходящих для управляемого расписанием вызова: подпрограммы пакетной обработки, например обновление списков связанных продуктов для пользователей на основе их последнего поведения, стандартные задачи обработки данных, например обновление индексов или формирование накопленных результатов, анализ данных для ежедневных отчетов, очистка хранения данных и проверка согласованности данных.</span><span class="sxs-lookup"><span data-stu-id="ac66d-151">Typical examples of tasks that are suited to schedule-driven invocation include batch-processing routines (such as updating related-products lists for users based on their recent behavior), routine data processing tasks (such as updating indexes or generating accumulated results), data analysis for daily reports, data retention cleanup, and data consistency checks.</span></span>

<span data-ttu-id="ac66d-152">При использовании управляемой расписанием задачи, которая должна выполняться как единственный экземпляр, надо учитывать следующее:</span><span class="sxs-lookup"><span data-stu-id="ac66d-152">If you use a schedule-driven task that must run as a single instance, be aware of the following:</span></span>

- <span data-ttu-id="ac66d-153">Если вычислительный экземпляр, в котором выполняется планировщик (например виртуальная машина с планировщиком заданий Windows), масштабируется, у вас будет выполняться несколько экземпляров планировщика,</span><span class="sxs-lookup"><span data-stu-id="ac66d-153">If the compute instance that is running the scheduler (such as a virtual machine using Windows scheduled tasks) is scaled, you will have multiple instances of the scheduler running.</span></span> <span data-ttu-id="ac66d-154">что может привести к запуску нескольких экземпляров задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-154">These could start multiple instances of the task.</span></span>
- <span data-ttu-id="ac66d-155">Если длительность выполнения задач превышает период между событиями планировщика, планировщик может запустить еще один экземпляр задачи, когда предыдущий еще выполняется.</span><span class="sxs-lookup"><span data-stu-id="ac66d-155">If tasks run for longer than the period between scheduler events, the scheduler may start another instance of the task while the previous one is still running.</span></span>

## <a name="returning-results"></a><span data-ttu-id="ac66d-156">Возвращение результатов</span><span class="sxs-lookup"><span data-stu-id="ac66d-156">Returning results</span></span>

<span data-ttu-id="ac66d-157">Фоновые задания выполняются асинхронно в отдельном процессе или даже отдельном расположении, из пользовательского интерфейса или процесса, который вызывает фоновое задание.</span><span class="sxs-lookup"><span data-stu-id="ac66d-157">Background jobs execute asynchronously in a separate process, or even in a separate location, from the UI or the process that invoked the background task.</span></span> <span data-ttu-id="ac66d-158">В идеальном случае фоновые задачи — это операции типа «отправить и забыть», и их выполнение не оказывает влияния на пользовательский интерфейс или вызывающий процесс.</span><span class="sxs-lookup"><span data-stu-id="ac66d-158">Ideally, background tasks are “fire and forget” operations, and their execution progress has no impact on the UI or the calling process.</span></span> <span data-ttu-id="ac66d-159">Это означает, что вызывающий процесс не ждет завершения задач</span><span class="sxs-lookup"><span data-stu-id="ac66d-159">This means that the calling process does not wait for completion of the tasks.</span></span> <span data-ttu-id="ac66d-160">и, следовательно, не может обнаружить автоматически, когда задача завершена.</span><span class="sxs-lookup"><span data-stu-id="ac66d-160">Therefore, it cannot automatically detect when the task ends.</span></span>

<span data-ttu-id="ac66d-161">Если требуется связать фоновую задачу с вызывающей задачей, чтобы она сообщала о ходе выполнения или завершении, для этого необходимо реализовать механизм.</span><span class="sxs-lookup"><span data-stu-id="ac66d-161">If you require a background task to communicate with the calling task to indicate progress or completion, you must implement a mechanism for this.</span></span> <span data-ttu-id="ac66d-162">Ниже приведены некоторые примеры.</span><span class="sxs-lookup"><span data-stu-id="ac66d-162">Some examples are:</span></span>

- <span data-ttu-id="ac66d-163">Значение индикатора состояния записи в хранилище, доступное в пользовательском интерфейсе или вызывающей задаче, которая может отслеживать или проверять это значение при необходимости.</span><span class="sxs-lookup"><span data-stu-id="ac66d-163">Write a status indicator value to storage that is accessible to the UI or caller task, which can monitor or check this value when required.</span></span> <span data-ttu-id="ac66d-164">Другие данные, которые фоновая задача должна возвращать вызывающей стороне, могут помещаться в то же хранилище.</span><span class="sxs-lookup"><span data-stu-id="ac66d-164">Other data that the background task must return to the caller can be placed into the same storage.</span></span>
- <span data-ttu-id="ac66d-165">Создание очереди ответов, которую прослушивает пользовательский интерфейс или вызывающая задача.</span><span class="sxs-lookup"><span data-stu-id="ac66d-165">Establish a reply queue that the UI or caller listens on.</span></span> <span data-ttu-id="ac66d-166">Фоновая задача может отправлять сообщения в очередь, сообщая о состоянии и завершении.</span><span class="sxs-lookup"><span data-stu-id="ac66d-166">The background task can send messages to the queue that indicate status and completion.</span></span> <span data-ttu-id="ac66d-167">Данные, которые фоновая задача должна возвращать вызывающей стороне, могут содержаться в сообщениях.</span><span class="sxs-lookup"><span data-stu-id="ac66d-167">Data that the background task must return to the caller can be placed into the messages.</span></span> <span data-ttu-id="ac66d-168">Если вы используете служебную шину Azure, то для реализации этой возможности можно использовать свойства **ReplyTo** и **CorrelationId**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-168">If you are using Azure Service Bus, you can use the **ReplyTo** and **CorrelationId** properties to implement this capability.</span></span>
- <span data-ttu-id="ac66d-169">Предоставление из фоновой задачи API или конечной точки, к которой может обратиться пользовательский интерфейс или вызывающая задача, чтобы получить информацию о состоянии.</span><span class="sxs-lookup"><span data-stu-id="ac66d-169">Expose an API or endpoint from the background task that the UI or caller can access to obtain status information.</span></span> <span data-ttu-id="ac66d-170">Данные, которые фоновая задача должна возвращать вызывающей стороне, могут содержаться в ответе.</span><span class="sxs-lookup"><span data-stu-id="ac66d-170">Data that the background task must return to the caller can be included in the response.</span></span>
- <span data-ttu-id="ac66d-171">Фоновая задача выполняет обратный вызов пользовательского интерфейса или вызывающей задачи через API, чтобы сообщить о состоянии в предопределенных точках или о завершении.</span><span class="sxs-lookup"><span data-stu-id="ac66d-171">Have the background task call back to the UI or caller through an API to indicate status at predefined points or on completion.</span></span> <span data-ttu-id="ac66d-172">Это может быть реализовано через локальные события или с помощью механизма публикации и подписки.</span><span class="sxs-lookup"><span data-stu-id="ac66d-172">This might be through events raised locally or through a publish-and-subscribe mechanism.</span></span> <span data-ttu-id="ac66d-173">Данные, которые фоновая задача должна возвращать вызывающей стороне, могут содержаться в запросе или полезных данных событий.</span><span class="sxs-lookup"><span data-stu-id="ac66d-173">Data that the background task must return to the caller can be included in the request or event payload.</span></span>

## <a name="hosting-environment"></a><span data-ttu-id="ac66d-174">Среда размещения</span><span class="sxs-lookup"><span data-stu-id="ac66d-174">Hosting environment</span></span>

<span data-ttu-id="ac66d-175">Вы можете размещать фоновые задачи с помощью ряда разных служб платформы Azure:</span><span class="sxs-lookup"><span data-stu-id="ac66d-175">You can host background tasks by using a range of different Azure platform services:</span></span>

- <span data-ttu-id="ac66d-176">[**Веб-приложения Azure и веб-задания**](#azure-web-apps-and-webjobs).</span><span class="sxs-lookup"><span data-stu-id="ac66d-176">[**Azure Web Apps and WebJobs**](#azure-web-apps-and-webjobs).</span></span> <span data-ttu-id="ac66d-177">Вы можете использовать веб-задания, чтобы выполнять пользовательские задания на основе диапазона различных типов сценариев или исполняемых файлов в контексте веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-177">You can use WebJobs to execute custom jobs based on a range of different types of scripts or executable programs within the context of a web app.</span></span>
- <span data-ttu-id="ac66d-178">[**Виртуальные машины Azure**](#azure-virtual-machines).</span><span class="sxs-lookup"><span data-stu-id="ac66d-178">[**Azure Virtual Machines**](#azure-virtual-machines).</span></span> <span data-ttu-id="ac66d-179">Если вы используете службу Windows или хотите использовать планировщик заданий Windows, обычно для размещения фоновых задач используется выделенная виртуальная машина.</span><span class="sxs-lookup"><span data-stu-id="ac66d-179">If you have a Windows service or want to use the Windows Task Scheduler, it is common to host your background tasks within a dedicated virtual machine.</span></span>
- <span data-ttu-id="ac66d-180">[**Пакетная служба Azure**](#azure-batch).</span><span class="sxs-lookup"><span data-stu-id="ac66d-180">[**Azure Batch**](#azure-batch).</span></span> <span data-ttu-id="ac66d-181">Пакетная служба — это платформа, которая позволяет запланировать выполнение ресурсоемких вычислений в управляемой коллекции виртуальных машин.</span><span class="sxs-lookup"><span data-stu-id="ac66d-181">Batch is a platform service that schedules compute-intensive work to run on a managed collection of virtual machines.</span></span> <span data-ttu-id="ac66d-182">Она поддерживает автоматическое масштабирование вычислительных ресурсов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-182">It can automatically scale compute resources.</span></span>
- <span data-ttu-id="ac66d-183">[**Служба Azure Kubernetes**](#azure-kubernetes-service) (AKS).</span><span class="sxs-lookup"><span data-stu-id="ac66d-183">[**Azure Kubernetes Service**](#azure-kubernetes-service) (AKS).</span></span> <span data-ttu-id="ac66d-184">Служба Azure Kubernetes предоставляет управляемую среду размещения для Kubernetes в Azure.</span><span class="sxs-lookup"><span data-stu-id="ac66d-184">Azure Kubernetes Service provides a managed hosting environment for Kubernetes on Azure.</span></span>

<span data-ttu-id="ac66d-185">В следующих разделах более подробно описан каждый из этих вариантов, а также приведены рекомендации по их выбору.</span><span class="sxs-lookup"><span data-stu-id="ac66d-185">The following sections describe each of these options in more detail, and include considerations to help you choose the appropriate option.</span></span>

### <a name="azure-web-apps-and-webjobs"></a><span data-ttu-id="ac66d-186">Веб-приложения Azure и веб-задания</span><span class="sxs-lookup"><span data-stu-id="ac66d-186">Azure Web Apps and WebJobs</span></span>

<span data-ttu-id="ac66d-187">Вы можете использовать веб-задания Azure для выполнения пользовательских заданий как фоновых задач в веб-приложении Azure.</span><span class="sxs-lookup"><span data-stu-id="ac66d-187">You can use Azure WebJobs to execute custom jobs as background tasks within an Azure Web App.</span></span> <span data-ttu-id="ac66d-188">Веб-задания выполняются в контексте веб-приложения непрерывно</span><span class="sxs-lookup"><span data-stu-id="ac66d-188">WebJobs run within the context of your web app as a continuous process.</span></span> <span data-ttu-id="ac66d-189">или в ответ на событие триггера из планировщика Azure либо на какие-либо внешние факторы, например изменения в хранилище больших двоичных объектов и очередях сообщений.</span><span class="sxs-lookup"><span data-stu-id="ac66d-189">WebJobs also run in response to a trigger event from Azure Scheduler or external factors, such as changes to storage blobs and message queues.</span></span> <span data-ttu-id="ac66d-190">Задания можно запускать и останавливать по запросу, а также корректно завершать.</span><span class="sxs-lookup"><span data-stu-id="ac66d-190">Jobs can be started and stopped on demand, and shut down gracefully.</span></span> <span data-ttu-id="ac66d-191">При сбое непрерывно выполняемого веб-задания оно автоматически перезапускается.</span><span class="sxs-lookup"><span data-stu-id="ac66d-191">If a continuously running WebJob fails, it is automatically restarted.</span></span> <span data-ttu-id="ac66d-192">Действия при повторной попытке и ошибке можно настраивать.</span><span class="sxs-lookup"><span data-stu-id="ac66d-192">Retry and error actions are configurable.</span></span>

<span data-ttu-id="ac66d-193">При настройке веб-задания:</span><span class="sxs-lookup"><span data-stu-id="ac66d-193">When you configure a WebJob:</span></span>

- <span data-ttu-id="ac66d-194">Если требуется, чтобы задание реагировало на управляемый событиями триггер, необходимо выбрать параметр **Выполнять непрерывно**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-194">If you want the job to respond to an event-driven trigger, you should configure it as **Run continuously**.</span></span> <span data-ttu-id="ac66d-195">Сценарий или программа хранится в папке site/wwwroot/app_data/jobs/continuous.</span><span class="sxs-lookup"><span data-stu-id="ac66d-195">The script or program is stored in the folder named site/wwwroot/app_data/jobs/continuous.</span></span>
- <span data-ttu-id="ac66d-196">Если требуется, чтобы задание реагировало на триггер, управляемый событиями, необходимо выбрать параметр **Выполнять по расписанию**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-196">If you want the job to respond to a schedule-driven trigger, you should configure it as **Run on a schedule**.</span></span> <span data-ttu-id="ac66d-197">Сценарий или программа хранится в папке site/wwwroot/app_data/jobs/triggered.</span><span class="sxs-lookup"><span data-stu-id="ac66d-197">The script or program is stored in the folder named site/wwwroot/app_data/jobs/triggered.</span></span>
- <span data-ttu-id="ac66d-198">Если при настройке задания выбрать параметр **Выполнять по требованию**, оно будет выполнять тот же код, что и параметр **Выполнять по расписанию**, когда вы его запустите.</span><span class="sxs-lookup"><span data-stu-id="ac66d-198">If you choose the **Run on demand** option when you configure a job, it will execute the same code as the **Run on a schedule** option when you start it.</span></span>

<span data-ttu-id="ac66d-199">Веб-задания Azure выполняются в песочнице веб-приложения,</span><span class="sxs-lookup"><span data-stu-id="ac66d-199">Azure WebJobs run within the sandbox of the web app.</span></span> <span data-ttu-id="ac66d-200">а значит, они могут обращаться к переменным среды и обмениваться с веб-приложением такой информацией, как строки подключения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-200">This means that they can access environment variables and share information, such as connection strings, with the web app.</span></span> <span data-ttu-id="ac66d-201">Задание имеет доступ к уникальному идентификатору компьютера, на котором оно выполняется.</span><span class="sxs-lookup"><span data-stu-id="ac66d-201">The job has access to the unique identifier of the machine that is running the job.</span></span> <span data-ttu-id="ac66d-202">Строка подключения **AzureWebJobsStorage** предоставляет доступ к очередям, большим двоичным объектам и таблицам службы хранилища Azure для обращения к данным приложений, а также к служебной шине для обмена данными и сообщениями.</span><span class="sxs-lookup"><span data-stu-id="ac66d-202">The connection string named **AzureWebJobsStorage** provides access to Azure storage queues, blobs, and tables for application data, and access to Service Bus for messaging and communication.</span></span> <span data-ttu-id="ac66d-203">Строка подключения **AzureWebJobsStorage** обеспечивает доступ к файлам журнала действий задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-203">The connection string named **AzureWebJobsDashboard** provides access to the job action log files.</span></span>

<span data-ttu-id="ac66d-204">Веб-задания Azure обладают следующими характеристиками:</span><span class="sxs-lookup"><span data-stu-id="ac66d-204">Azure WebJobs have the following characteristics:</span></span>

- <span data-ttu-id="ac66d-205">**Безопасность.** Веб-задания защищены с помощью учетных данных развертывания веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-205">**Security**: WebJobs are protected by the deployment credentials of the web app.</span></span>
- <span data-ttu-id="ac66d-206">**Типы поддерживаемых файлов.** Веб-задания можно определять с помощью командных скриптов (CMD-файлы), пакетных файлов (BAT-файлы), сценариев PowerShell (PS1-файлы), скриптов оболочки Bash (SH-файлы), скриптов PHP (PHP-файлы), скриптов Python (PY-файлы), кода JavaScript (JS-файлы) и исполняемых программ (EXE-, JAR-файлы и многие другие).</span><span class="sxs-lookup"><span data-stu-id="ac66d-206">**Supported file types**: You can define WebJobs by using command scripts (.cmd), batch files (.bat), PowerShell scripts (.ps1), bash shell scripts (.sh), PHP scripts (.php), Python scripts (.py), JavaScript code (.js), and executable programs (.exe, .jar, and more).</span></span>
- <span data-ttu-id="ac66d-207">**Развертывание.** Сценарии и исполняемые файлы можно развертывать с помощью [портала Azure](/azure/app-service-web/web-sites-create-web-jobs), [Visual Studio](/azure/app-service-web/websites-dotnet-deploy-webjobs), [пакета SDK для веб-заданий Azure](/azure/app-service/webjobs-sdk-get-started) или посредством прямого копирования в следующие расположения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-207">**Deployment**: You can deploy scripts and executables by using the [Azure portal](/azure/app-service-web/web-sites-create-web-jobs), by using [Visual Studio](/azure/app-service-web/websites-dotnet-deploy-webjobs), by using the [Azure WebJobs SDK](/azure/app-service/webjobs-sdk-get-started), or by copying them directly to the following locations:</span></span>
  - <span data-ttu-id="ac66d-208">Для выполнения по триггеру: site/wwwroot/app_data/jobs/triggered/{имя задания}</span><span class="sxs-lookup"><span data-stu-id="ac66d-208">For triggered execution: site/wwwroot/app_data/jobs/triggered/{job name}</span></span>
  - <span data-ttu-id="ac66d-209">Для непрерывного выполнения: site/wwwroot/app_data/jobs/continuous/{имя задания}</span><span class="sxs-lookup"><span data-stu-id="ac66d-209">For continuous execution: site/wwwroot/app_data/jobs/continuous/{job name}</span></span>
- <span data-ttu-id="ac66d-210">**Ведение журналов.** Console.Out обрабатывается (помечается) как INFO (информация),</span><span class="sxs-lookup"><span data-stu-id="ac66d-210">**Logging**: Console.Out is treated (marked) as INFO.</span></span> <span data-ttu-id="ac66d-211">а Console.Error — как ошибка.</span><span class="sxs-lookup"><span data-stu-id="ac66d-211">Console.Error is treated as ERROR.</span></span> <span data-ttu-id="ac66d-212">К данным наблюдения и диагностики можно обратиться с помощью портала Azure,</span><span class="sxs-lookup"><span data-stu-id="ac66d-212">You can access monitoring and diagnostics information by using the Azure portal.</span></span> <span data-ttu-id="ac66d-213">а файлы журнала скачать непосредственно с сайта.</span><span class="sxs-lookup"><span data-stu-id="ac66d-213">You can download log files directly from the site.</span></span> <span data-ttu-id="ac66d-214">Они сохраняются в следующих расположениях:</span><span class="sxs-lookup"><span data-stu-id="ac66d-214">They are saved in the following locations:</span></span>
  - <span data-ttu-id="ac66d-215">Для выполнения по триггеру: Vfs/data/jobs/triggered/jobName</span><span class="sxs-lookup"><span data-stu-id="ac66d-215">For triggered execution: Vfs/data/jobs/triggered/jobName</span></span>
  - <span data-ttu-id="ac66d-216">Для непрерывного выполнения: Vfs/data/jobs/continuous/jobName</span><span class="sxs-lookup"><span data-stu-id="ac66d-216">For continuous execution: Vfs/data/jobs/continuous/jobName</span></span>
- <span data-ttu-id="ac66d-217">**Конфигурация.** Веб-задания можно настраивать с помощью портала, REST API и PowerShell.</span><span class="sxs-lookup"><span data-stu-id="ac66d-217">**Configuration**: You can configure WebJobs by using the portal, the REST API, and PowerShell.</span></span> <span data-ttu-id="ac66d-218">Файл конфигурации settings.job, находящийся в том же корневом каталоге, что и сценарий задания, можно использовать для предоставления данных о конфигурации задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-218">You can use a configuration file named settings.job in the same root directory as the job script to provide configuration information for a job.</span></span> <span data-ttu-id="ac66d-219">Например: </span><span class="sxs-lookup"><span data-stu-id="ac66d-219">For example:</span></span>
  - <span data-ttu-id="ac66d-220">{ "stopping_wait_time": 60 }</span><span class="sxs-lookup"><span data-stu-id="ac66d-220">{ "stopping_wait_time": 60 }</span></span>
  - <span data-ttu-id="ac66d-221">{ "is_singleton": true }</span><span class="sxs-lookup"><span data-stu-id="ac66d-221">{ "is_singleton": true }</span></span>

#### <a name="considerations"></a><span data-ttu-id="ac66d-222">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="ac66d-222">Considerations</span></span>

- <span data-ttu-id="ac66d-223">По умолчанию веб-задания масштабируются вместе с веб-приложением.</span><span class="sxs-lookup"><span data-stu-id="ac66d-223">By default, WebJobs scale with the web app.</span></span> <span data-ttu-id="ac66d-224">Однако задания можно настроить для запуска на одном экземпляре, задав для свойства конфигурации **is_singleton** значение **true**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-224">However, you can configure jobs to run on single instance by setting the **is_singleton** configuration property to **true**.</span></span> <span data-ttu-id="ac66d-225">Одноэкземплярные веб-задания удобны для задач, которые не требуется масштабировать или выполнять одновременно в виде нескольких экземпляров: это может быть повторная индексация, анализ данных и схожие задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-225">Single instance WebJobs are useful for tasks that you do not want to scale or run as simultaneous multiple instances, such as reindexing, data analysis, and similar tasks.</span></span>
- <span data-ttu-id="ac66d-226">Чтобы свести к минимуму влияние заданий на производительность веб-приложения, рассмотрите возможность создания пустого экземпляра веб-приложения Azure в новом плане службы приложений, чтобы размещать веб-задания, которые могут долго выполняться или потреблять много ресурсов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-226">To minimize the impact of jobs on the performance of the web app, consider creating an empty Azure Web App instance in a new App Service plan to host WebJobs that may be long running or resource intensive.</span></span>

### <a name="azure-virtual-machines"></a><span data-ttu-id="ac66d-227">Виртуальные машины Azure</span><span class="sxs-lookup"><span data-stu-id="ac66d-227">Azure Virtual Machines</span></span>

<span data-ttu-id="ac66d-228">Фоновые задачи могут быть реализованы таким способом, который препятствует их развертыванию в веб-приложениях, или эти варианты могут оказаться неудобными.</span><span class="sxs-lookup"><span data-stu-id="ac66d-228">Background tasks might be implemented in a way that prevents them from being deployed to Azure Web Apps, or these options might not be convenient.</span></span> <span data-ttu-id="ac66d-229">Типичные примеры — службы Windows, служебные программы и исполняемые программы сторонних производителей.</span><span class="sxs-lookup"><span data-stu-id="ac66d-229">Typical examples are Windows services, and third-party utilities and executable programs.</span></span> <span data-ttu-id="ac66d-230">Другим примером могут служить программы, написанные для среды выполнения, отличной от среды, в которой размещено приложение.</span><span class="sxs-lookup"><span data-stu-id="ac66d-230">Another example might be programs written for an execution environment that is different than that hosting the application.</span></span> <span data-ttu-id="ac66d-231">Например, это может быть программа Unix или Linux, которую необходимо выполнить из приложения Windows или .NET.</span><span class="sxs-lookup"><span data-stu-id="ac66d-231">For example, it might be a Unix or Linux program that you want to execute from a Windows or .NET application.</span></span> <span data-ttu-id="ac66d-232">Вы можете выбрать из диапазона операционных систем для виртуальной машины Azure и запустить свою службу или исполняемый файл на этой виртуальной машине.</span><span class="sxs-lookup"><span data-stu-id="ac66d-232">You can choose from a range of operating systems for an Azure virtual machine, and run your service or executable on that virtual machine.</span></span>

<span data-ttu-id="ac66d-233">Чтобы решить, когда следует использовать виртуальные машины, см. статью [Сравнение служб приложений, облачных служб и виртуальных машин Azure](/azure/app-service-web/choose-web-site-cloud-service-vm/).</span><span class="sxs-lookup"><span data-stu-id="ac66d-233">To help you choose when to use Virtual Machines, see [Azure App Services, Cloud Services and Virtual Machines comparison](/azure/app-service-web/choose-web-site-cloud-service-vm/).</span></span> <span data-ttu-id="ac66d-234">Дополнительные сведения о параметрах виртуальных машин см. в статье [Размеры виртуальных машин Windows в Azure](/azure/virtual-machines/windows/sizes).</span><span class="sxs-lookup"><span data-stu-id="ac66d-234">For information about the options for Virtual Machines, see [Sizes for Windows virtual machines in Azure](/azure/virtual-machines/windows/sizes).</span></span> <span data-ttu-id="ac66d-235">Дополнительные сведения об операционных системах и готовых образах, доступных для виртуальных машин, см. в [магазине виртуальных машин Azure](https://azure.microsoft.com/gallery/virtual-machines/).</span><span class="sxs-lookup"><span data-stu-id="ac66d-235">For more information about the operating systems and prebuilt images that are available for Virtual Machines, see [Azure Virtual Machines Marketplace](https://azure.microsoft.com/gallery/virtual-machines/).</span></span>

<span data-ttu-id="ac66d-236">Для запуска фоновой задачи в отдельной виртуальной машине имеется целый ряд возможностей:</span><span class="sxs-lookup"><span data-stu-id="ac66d-236">To initiate the background task in a separate virtual machine, you have a range of options:</span></span>

- <span data-ttu-id="ac66d-237">Задачу можно выполнять по запросу непосредственно из приложения, отправляя запрос к конечной точке, которую предоставляет задача.</span><span class="sxs-lookup"><span data-stu-id="ac66d-237">You can execute the task on demand directly from your application by sending a request to an endpoint that the task exposes.</span></span> <span data-ttu-id="ac66d-238">При этом передаются все необходимые задаче данные.</span><span class="sxs-lookup"><span data-stu-id="ac66d-238">This passes in any data that the task requires.</span></span> <span data-ttu-id="ac66d-239">Эта конечная точка вызывает задачу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-239">This endpoint invokes the task.</span></span>
- <span data-ttu-id="ac66d-240">Можно настроить задачу для запуска по расписанию с помощью планировщика или таймера, доступных в выбранной операционной системе.</span><span class="sxs-lookup"><span data-stu-id="ac66d-240">You can configure the task to run on a schedule by using a scheduler or timer that is available in your chosen operating system.</span></span> <span data-ttu-id="ac66d-241">Например, в Windows для выполнения сценариев и задач можно использовать планировщик заданий Windows.</span><span class="sxs-lookup"><span data-stu-id="ac66d-241">For example, on Windows you can use Windows Task Scheduler to execute scripts and tasks.</span></span> <span data-ttu-id="ac66d-242">Если на виртуальной машине установлен SQL Server, для этой цели можно также использовать агент SQL Server.</span><span class="sxs-lookup"><span data-stu-id="ac66d-242">Or, if you have SQL Server installed on the virtual machine, you can use the SQL Server Agent to execute scripts and tasks.</span></span>
- <span data-ttu-id="ac66d-243">Запустить задачу с помощью планировщика Azure можно, добавив сообщение в очередь, которую задача прослушивает, или отправив запрос к API, предоставляемому задачей.</span><span class="sxs-lookup"><span data-stu-id="ac66d-243">You can use Azure Scheduler to initiate the task by adding a message to a queue that the task listens on, or by sending a request to an API that the task exposes.</span></span>

<span data-ttu-id="ac66d-244">В предыдущем разделе [Триггеры](#triggers) вы можете ознакомиться с дополнительной информацией о том, как можно инициировать фоновые задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-244">See the earlier section [Triggers](#triggers) for more information about how you can initiate background tasks.</span></span>

<!-- markdownlint-disable MD024 -->

#### <a name="considerations"></a><span data-ttu-id="ac66d-245">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="ac66d-245">Considerations</span></span>

<!-- markdownlint-enable MD024 -->

<span data-ttu-id="ac66d-246">Принимая решение о развертывании фоновых задач в виртуальной машине Azure, необходимо учитывать следующее:</span><span class="sxs-lookup"><span data-stu-id="ac66d-246">Consider the following points when you are deciding whether to deploy background tasks in an Azure virtual machine:</span></span>

- <span data-ttu-id="ac66d-247">Размещение фоновых задач в отдельной виртуальной машине Azure обеспечивает гибкость и позволяет точно контролировать запуск, выполнение, планирование и распределение ресурсов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-247">Hosting background tasks in a separate Azure virtual machine provides flexibility and allows precise control over initiation, execution, scheduling, and resource allocation.</span></span> <span data-ttu-id="ac66d-248">Тем не менее это увеличивает затраты времени выполнения, если виртуальную машину необходимо разворачивать только для фоновых задач.</span><span class="sxs-lookup"><span data-stu-id="ac66d-248">However, it will increase runtime cost if a virtual machine must be deployed just to run background tasks.</span></span>
- <span data-ttu-id="ac66d-249">На портале Azure не предусмотрены средства для мониторинга задач и возможность автоматического перезапуска невыполненных задач. Но вы можете отслеживать основные данные о состоянии виртуальной машины и управлять ею с помощью [командлетов Azure Resource Manager](https://msdn.microsoft.com/library/mt125356.aspx).</span><span class="sxs-lookup"><span data-stu-id="ac66d-249">There is no facility to monitor the tasks in the Azure portal and no automated restart capability for failed tasks--although you can monitor the basic status of the virtual machine and manage it by using the  [Azure Resource Manager Cmdlets](https://msdn.microsoft.com/library/mt125356.aspx).</span></span> <span data-ttu-id="ac66d-250">Однако не существует средств для управления процессами и потоками в вычислительных узлах.</span><span class="sxs-lookup"><span data-stu-id="ac66d-250">However, there are no facilities to control processes and threads in compute nodes.</span></span> <span data-ttu-id="ac66d-251">Как правило, использование виртуальной машины требует дополнительных усилий, чтобы реализовать механизм, который собирает данные из инструментария в задаче, а также из операционной системы в виртуальной машине.</span><span class="sxs-lookup"><span data-stu-id="ac66d-251">Typically, using a virtual machine will require additional effort to implement a mechanism that collects data from instrumentation in the task, and from the operating system in the virtual machine.</span></span> <span data-ttu-id="ac66d-252">Одно из решений, которое может подойти, — использовать [пакет управления System Center для Azure](https://www.microsoft.com/download/details.aspx?id=50013).</span><span class="sxs-lookup"><span data-stu-id="ac66d-252">One solution that might be appropriate is to use the [System Center Management Pack for Azure](https://www.microsoft.com/download/details.aspx?id=50013).</span></span>
- <span data-ttu-id="ac66d-253">Вы можете рассмотреть создание зондов мониторинга, предоставляемых через конечные точки HTTP.</span><span class="sxs-lookup"><span data-stu-id="ac66d-253">You might consider creating monitoring probes that are exposed through HTTP endpoints.</span></span> <span data-ttu-id="ac66d-254">Код этих зондов может проверять работоспособность, собирать информацию об операциях и статистические данные или сопоставлять информацию об ошибках и возвращать ее в приложение управления.</span><span class="sxs-lookup"><span data-stu-id="ac66d-254">The code for these probes could perform health checks, collect operational information and statistics--or collate error information and return it to a management application.</span></span> <span data-ttu-id="ac66d-255">Дополнительную информацию см. в статье [Шаблон мониторинга конечных точек работоспособности](../patterns/health-endpoint-monitoring.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-255">For more information, see the [Health Endpoint Monitoring pattern](../patterns/health-endpoint-monitoring.md).</span></span>

<span data-ttu-id="ac66d-256">Дополнительные сведения можно найти в разделе </span><span class="sxs-lookup"><span data-stu-id="ac66d-256">For more information, see:</span></span>

- [<span data-ttu-id="ac66d-257">Виртуальные машины</span><span class="sxs-lookup"><span data-stu-id="ac66d-257">Virtual Machines</span></span>](https://azure.microsoft.com/services/virtual-machines/)
- [<span data-ttu-id="ac66d-258">Виртуальные машины Azure. Вопросы и ответы</span><span class="sxs-lookup"><span data-stu-id="ac66d-258">Azure Virtual Machines FAQ</span></span>](/azure/virtual-machines/virtual-machines-linux-classic-faq?toc=%2fazure%2fvirtual-machines%2flinux%2fclassic%2ftoc.json)

### <a name="azure-batch"></a><span data-ttu-id="ac66d-259">Пакетная служба Azure</span><span class="sxs-lookup"><span data-stu-id="ac66d-259">Azure Batch</span></span>

<span data-ttu-id="ac66d-260">Рекомендуем использовать [пакетную службу Azure](/azure/batch/) для параллельного выполнения больших рабочих нагрузок с высокопроизводительными вычислениями (HPC) на нескольких десятках, сотнях или тысячах виртуальных машин.</span><span class="sxs-lookup"><span data-stu-id="ac66d-260">Consider [Azure Batch](/azure/batch/) if you need to run large, parallel high-performance computing (HPC) workloads across tens, hundreds, or thousands of VMs.</span></span>

<span data-ttu-id="ac66d-261">Пакетная служба подготавливает виртуальные машины, распределяет между ними задачи, запускает задачи и отслеживает ход выполнения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-261">The Batch service provisions the VMs, assign tasks to the VMs, runs the tasks, and monitors the progress.</span></span> <span data-ttu-id="ac66d-262">Пакетная служба может автоматически масштабировать виртуальные машины при изменении рабочей нагрузки.</span><span class="sxs-lookup"><span data-stu-id="ac66d-262">Batch can automatically scale out the VMs in response to the workload.</span></span> <span data-ttu-id="ac66d-263">Кроме того, пакетная служба поддерживает планирование заданий.</span><span class="sxs-lookup"><span data-stu-id="ac66d-263">Batch also provides job scheduling.</span></span> <span data-ttu-id="ac66d-264">Пакетная служба Azure поддерживает виртуальные машины Windows и Linux.</span><span class="sxs-lookup"><span data-stu-id="ac66d-264">Azure Batch supports both Linux and Windows VMs.</span></span>

<!-- markdownlint-disable MD024 -->

#### <a name="considerations"></a><span data-ttu-id="ac66d-265">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="ac66d-265">Considerations</span></span>

<!-- markdownlint-enable MD024 -->

<span data-ttu-id="ac66d-266">Пакетная служба отлично работает с реальными параллельными рабочими нагрузками.</span><span class="sxs-lookup"><span data-stu-id="ac66d-266">Batch works well with intrinsically parallel workloads.</span></span> <span data-ttu-id="ac66d-267">Также она позволяет выполнять параллельные вычисления с завершающим шагом редукции или [приложения интерфейса передачи сообщений](/azure/batch/batch-mpi) для параллельных задач, в которых требуется передача сообщений между узлами.</span><span class="sxs-lookup"><span data-stu-id="ac66d-267">It can also perform parallel calculations with a reduce step at the end, or run [Message Passing Interface (MPI) applications](/azure/batch/batch-mpi) for parallel tasks that require message passing between nodes.</span></span>

<span data-ttu-id="ac66d-268">Задания пакетной службы Azure выполняются в пуле узлов (виртуальных машин).</span><span class="sxs-lookup"><span data-stu-id="ac66d-268">An Azure Batch job runs on a pool of nodes (VMs).</span></span> <span data-ttu-id="ac66d-269">Один из возможных подходов — выделять этот пул только при необходимости и удалять его по завершении задания.</span><span class="sxs-lookup"><span data-stu-id="ac66d-269">One approach is to allocate a pool only when needed and then delete it after the job completes.</span></span> <span data-ttu-id="ac66d-270">Это позволяет более эффективно использовать ресурсы, так как узлы никогда не простаивают, но задание будет выполняться не сразу, а после задержки на выделение узлов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-270">This maximizes utilization, because nodes are not idle, but the job must wait for nodes to be allocated.</span></span> <span data-ttu-id="ac66d-271">Второй вариант — создавать пул заранее.</span><span class="sxs-lookup"><span data-stu-id="ac66d-271">Alternatively, you can create a pool ahead of time.</span></span> <span data-ttu-id="ac66d-272">Такой подход позволит быстрее запустить задание, но может привести к бездействию узлов на протяжении определенного периода.</span><span class="sxs-lookup"><span data-stu-id="ac66d-272">That approach minimizes the time that it takes for a job to start, but can result in having nodes that sit idle.</span></span> <span data-ttu-id="ac66d-273">Дополнительные сведения см. в разделе [Время существования пула и вычислительного узла](/azure/batch/batch-api-basics#pool-and-compute-node-lifetime).</span><span class="sxs-lookup"><span data-stu-id="ac66d-273">For more information, see [Pool and compute node lifetime](/azure/batch/batch-api-basics#pool-and-compute-node-lifetime).</span></span>

<span data-ttu-id="ac66d-274">Дополнительные сведения можно найти в разделе </span><span class="sxs-lookup"><span data-stu-id="ac66d-274">For more information, see:</span></span>

- [<span data-ttu-id="ac66d-275">Что такое пакетная служба Azure</span><span class="sxs-lookup"><span data-stu-id="ac66d-275">What is Azure Batch?</span></span>](/azure/batch/batch-technical-overview)
- <span data-ttu-id="ac66d-276">[Разработка решений для крупномасштабных параллельных вычислений с использованием пакетной службы](/azure/batch/batch-api-basics).</span><span class="sxs-lookup"><span data-stu-id="ac66d-276">[Develop large-scale parallel compute solutions with Batch](/azure/batch/batch-api-basics)</span></span>
- <span data-ttu-id="ac66d-277">[Решения для пакетных и высокопроизводительных вычислений при крупномасштабных рабочих нагрузках](/azure/batch/batch-hpc-solutions).</span><span class="sxs-lookup"><span data-stu-id="ac66d-277">[Batch and HPC solutions for large-scale computing workloads](/azure/batch/batch-hpc-solutions)</span></span>

### <a name="azure-kubernetes-service"></a><span data-ttu-id="ac66d-278">Служба Azure Kubernetes</span><span class="sxs-lookup"><span data-stu-id="ac66d-278">Azure Kubernetes Service</span></span>

<span data-ttu-id="ac66d-279">Служба Azure Kubernetes (AKS) управляет размещенной средой Kubernetes, позволяя легко развертывать контейнерные приложения и управлять ими.</span><span class="sxs-lookup"><span data-stu-id="ac66d-279">Azure Kubernetes Service (AKS) manages your hosted Kubernetes environment, which makes it easy to deploy and manage containerized applications.</span></span>

<span data-ttu-id="ac66d-280">Контейнеры очень удобны для выполнения заданий в фоновом режиме.</span><span class="sxs-lookup"><span data-stu-id="ac66d-280">Containers can be useful for running background jobs.</span></span> <span data-ttu-id="ac66d-281">Вот некоторые преимущества этой службы:</span><span class="sxs-lookup"><span data-stu-id="ac66d-281">Some of the benefits include:</span></span>

- <span data-ttu-id="ac66d-282">Контейнеры поддерживают размещение высокой плотности.</span><span class="sxs-lookup"><span data-stu-id="ac66d-282">Containers support high-density hosting.</span></span> <span data-ttu-id="ac66d-283">Фоновую задачу можно изолировать в контейнере, при этом размещая на каждой виртуальной машине несколько контейнеров.</span><span class="sxs-lookup"><span data-stu-id="ac66d-283">You can isolate a background task in a container, while placing multiple containers in each VM.</span></span>
- <span data-ttu-id="ac66d-284">Оркестратор контейнеров обеспечивает внутреннюю балансировку нагрузки, настройку внутренней сети и выполнение других задач по настройке.</span><span class="sxs-lookup"><span data-stu-id="ac66d-284">The container orchestrator handles internal load balancing, configuring the internal network, and other configuration tasks.</span></span>
- <span data-ttu-id="ac66d-285">Контейнеры можно запускать и останавливать по мере необходимости.</span><span class="sxs-lookup"><span data-stu-id="ac66d-285">Containers can be started and stopped as needed.</span></span>
- <span data-ttu-id="ac66d-286">Реестр контейнеров Azure позволяет зарегистрировать контейнеры в пределах платформы Azure.</span><span class="sxs-lookup"><span data-stu-id="ac66d-286">Azure Container Registry allows you to register your containers inside Azure boundaries.</span></span> <span data-ttu-id="ac66d-287">Это дает дополнительные преимущества, касающиеся безопасности, конфиденциальности и близкого взаимодействия.</span><span class="sxs-lookup"><span data-stu-id="ac66d-287">This comes with security, privacy, and proximity benefits.</span></span>

<!-- markdownlint-disable MD024 -->

#### <a name="considerations"></a><span data-ttu-id="ac66d-288">Рекомендации</span><span class="sxs-lookup"><span data-stu-id="ac66d-288">Considerations</span></span>

<!-- markdownlint-enable MD024 -->

- <span data-ttu-id="ac66d-289">Важно понимать, как работает оркестратор контейнеров.</span><span class="sxs-lookup"><span data-stu-id="ac66d-289">Requires an understanding of how to use a container orchestrator.</span></span> <span data-ttu-id="ac66d-290">В зависимости от квалификации разработчиков его использование может представлять некоторые трудности.</span><span class="sxs-lookup"><span data-stu-id="ac66d-290">Depending on the skillset of your DevOps team, this may or may not be an issue.</span></span>

<span data-ttu-id="ac66d-291">Дополнительные сведения можно найти в разделе </span><span class="sxs-lookup"><span data-stu-id="ac66d-291">For more information, see:</span></span>

- [<span data-ttu-id="ac66d-292">Общие сведения о мониторинге контейнеров в Azure</span><span class="sxs-lookup"><span data-stu-id="ac66d-292">Overview of containers in Azure</span></span>](https://azure.microsoft.com/overview/containers/)

- <span data-ttu-id="ac66d-293">[Общие сведения о частных реестрах контейнеров Docker](/azure/container-registry/container-registry-intro).</span><span class="sxs-lookup"><span data-stu-id="ac66d-293">[Introduction to private Docker container registries](/azure/container-registry/container-registry-intro)</span></span>

## <a name="partitioning"></a><span data-ttu-id="ac66d-294">Секционирование</span><span class="sxs-lookup"><span data-stu-id="ac66d-294">Partitioning</span></span>

<span data-ttu-id="ac66d-295">Если вы решили включить фоновые задачи в существующий вычислительный экземпляр, необходимо учесть, как это повлияет на атрибуты качества вычислительного экземпляра и саму фоновую задачу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-295">If you decide to include background tasks within an existing compute instance, you must consider how this will affect the quality attributes of the compute instance and the background task itself.</span></span> <span data-ttu-id="ac66d-296">Эти факторы помогут решить, следует ли совместно размещать задачи с существующим вычислительным экземпляром или выделить им отдельный вычислительный экземпляр:</span><span class="sxs-lookup"><span data-stu-id="ac66d-296">These factors will help you to decide whether to colocate the tasks with the existing compute instance or separate them out into a separate compute instance:</span></span>

- <span data-ttu-id="ac66d-297">**Доступность.** Фоновым задачам может не требоваться такой же уровень доступности, что и другим частям приложения, в частности, пользовательскому интерфейсу и другим частям, непосредственно участвующим во взаимодействии с пользователем.</span><span class="sxs-lookup"><span data-stu-id="ac66d-297">**Availability**: Background tasks might not need to have the same level of availability as other parts of the application, in particular the UI and other parts that are directly involved in user interaction.</span></span> <span data-ttu-id="ac66d-298">Фоновые задачи могут быть менее чувствительны к задержкам, сбоям при повторных подключениях и другим факторам, влияющим на доступность, так как операции могут быть поставлены в очередь.</span><span class="sxs-lookup"><span data-stu-id="ac66d-298">Background tasks might be more tolerant of latency, retried connection failures, and other factors that affect availability because the operations can be queued.</span></span> <span data-ttu-id="ac66d-299">Однако требуются достаточные ресурсы, чтобы предотвратить накопление запросов, которые могут блокировать очереди и влиять на приложение в целом.</span><span class="sxs-lookup"><span data-stu-id="ac66d-299">However, there must be sufficient capacity to prevent the backup of requests that could block queues and affect the application as a whole.</span></span>

- <span data-ttu-id="ac66d-300">**Масштабируемость.** Вероятно, у фоновых задач будут разные требования к масштабируемости пользовательского интерфейса и интерактивных частей приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-300">**Scalability**: Background tasks are likely to have a different scalability requirement than the UI and the interactive parts of the application.</span></span> <span data-ttu-id="ac66d-301">Масштабирование пользовательского интерфейса может потребоваться для обработки всплесков нагрузки по запросу, тогда как невыполненные фоновые задачи могут быть осуществлены в периоды меньшей загруженности меньшим числом вычислительных экземпляров.</span><span class="sxs-lookup"><span data-stu-id="ac66d-301">Scaling the UI might be necessary to meet peaks in demand, while outstanding background tasks might be completed during less busy times by a fewer number of compute instances.</span></span>

- <span data-ttu-id="ac66d-302">**Устойчивость.** Сбой вычислительного экземпляра, в котором просто размещены фоновые задачи, может не привести к неустранимым последствиям для приложения в целом, если запросы на выполнение этих задач можно поставить в очередь или отложить до того времени, пока задача снова не станет доступна.</span><span class="sxs-lookup"><span data-stu-id="ac66d-302">**Resiliency**: Failure of a compute instance that just hosts background tasks might not fatally affect the application as a whole if the requests for these tasks can be queued or postponed until the task is available again.</span></span> <span data-ttu-id="ac66d-303">Если за соответствующий интервал времени можно перезапустить вычислительный экземпляр и (или) задачи, на работе пользователей приложения это не отразится.</span><span class="sxs-lookup"><span data-stu-id="ac66d-303">If the compute instance and/or tasks can be restarted within an appropriate interval, users of the application might not be affected.</span></span>

- <span data-ttu-id="ac66d-304">**Безопасность.** У фоновых задач могут быть иные ограничения или требования к безопасности, чем у пользовательского интерфейса или других частей приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-304">**Security**: Background tasks might have different security requirements or restrictions than the UI or other parts of the application.</span></span> <span data-ttu-id="ac66d-305">Используя отдельный вычислительный экземпляр, можно указать другую среду безопасности для задач.</span><span class="sxs-lookup"><span data-stu-id="ac66d-305">By using a separate compute instance, you can specify a different security environment for the tasks.</span></span> <span data-ttu-id="ac66d-306">Также можно использовать шаблоны, например шаблон привратника, чтобы изолировать фоновые вычислительные экземпляры от пользовательского интерфейса. Это обеспечит максимальную безопасность и разделение.</span><span class="sxs-lookup"><span data-stu-id="ac66d-306">You can also use patterns such as Gatekeeper to isolate the background compute instances from the UI in order to maximize security and separation.</span></span>

- <span data-ttu-id="ac66d-307">**Производительность.** Можно выбрать тип вычислительного экземпляра для фоновых задач, соответствующий требованиям к производительности задач.</span><span class="sxs-lookup"><span data-stu-id="ac66d-307">**Performance**: You can choose the type of compute instance for background tasks to specifically match the performance requirements of the tasks.</span></span> <span data-ttu-id="ac66d-308">Это может означать использование более дешевого варианта вычислений, если задачи не требуют таких же возможностей обработки, что и пользовательский интерфейс, либо использование более крупного экземпляра, если они требуют дополнительной емкости и ресурсов.</span><span class="sxs-lookup"><span data-stu-id="ac66d-308">This might mean using a less expensive compute option if the tasks do not require the same processing capabilities as the UI, or a larger instance if they require additional capacity and resources.</span></span>

- <span data-ttu-id="ac66d-309">**Управляемость.** У фоновых задач может быть иной темп разработки и развертывания, чем у кода или пользовательского интерфейса основного приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-309">**Manageability**: Background tasks might have a different development and deployment rhythm from the main application code or the UI.</span></span> <span data-ttu-id="ac66d-310">Их развертывание в отдельном вычислительном экземпляре может упростить обновление и управление версиями.</span><span class="sxs-lookup"><span data-stu-id="ac66d-310">Deploying them to a separate compute instance can simplify updates and versioning.</span></span>

- <span data-ttu-id="ac66d-311">**Затраты.** Добавление вычислительных экземпляров для выполнения фоновых задач увеличивает расходы на размещение.</span><span class="sxs-lookup"><span data-stu-id="ac66d-311">**Cost**: Adding compute instances to execute background tasks increases hosting costs.</span></span> <span data-ttu-id="ac66d-312">Необходимо тщательно обдумать компромисс между дополнительной емкостью и этими дополнительными затратами.</span><span class="sxs-lookup"><span data-stu-id="ac66d-312">You should carefully consider the trade-off between additional capacity and these extra costs.</span></span>

<span data-ttu-id="ac66d-313">Дополнительные сведения см. в статьях [Шаблон выбора лидера](../patterns/leader-election.md) и [Шаблон конкурирующих потребителей](../patterns/competing-consumers.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-313">For more information, see the [Leader Election pattern](../patterns/leader-election.md) and the [Competing Consumers pattern](../patterns/competing-consumers.md).</span></span>

## <a name="conflicts"></a><span data-ttu-id="ac66d-314">Конфликты</span><span class="sxs-lookup"><span data-stu-id="ac66d-314">Conflicts</span></span>

<span data-ttu-id="ac66d-315">Если имеется несколько экземпляров фонового задания, возможно, они будут конкурировать за доступ к ресурсам и службам, например базам данных и хранилищу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-315">If you have multiple instances of a background job, it is possible that they will compete for access to resources and services, such as databases and storage.</span></span> <span data-ttu-id="ac66d-316">Такой одновременный доступ может привести к состязанию за ресурсы, что в состоянии вызвать конфликты доступности служб и целостности данных в хранилище.</span><span class="sxs-lookup"><span data-stu-id="ac66d-316">This concurrent access can result in resource contention, which might cause conflicts in availability of the services and in the integrity of data in storage.</span></span> <span data-ttu-id="ac66d-317">Состязание за ресурсы можно разрешить по принципу пессимистической блокировки,</span><span class="sxs-lookup"><span data-stu-id="ac66d-317">You can resolve resource contention by using a pessimistic locking approach.</span></span> <span data-ttu-id="ac66d-318">чтобы предотвратить одновременный доступ к службе конкурирующих экземпляров задачи или повреждение данных.</span><span class="sxs-lookup"><span data-stu-id="ac66d-318">This prevents competing instances of a task from concurrently accessing a service or corrupting data.</span></span>

<span data-ttu-id="ac66d-319">Другой метод разрешения конфликтов — определить фоновые задачи как единственный экземпляр, тогда в любой момент выполняться будет только один экземпляр.</span><span class="sxs-lookup"><span data-stu-id="ac66d-319">Another approach to resolve conflicts is to define background tasks as a singleton, so that there is only ever one instance running.</span></span> <span data-ttu-id="ac66d-320">Однако это устраняет надежность и преимущества производительности, свойственные конфигурации с несколькими экземплярами,</span><span class="sxs-lookup"><span data-stu-id="ac66d-320">However, this eliminates the reliability and performance benefits that a multiple-instance configuration can provide.</span></span> <span data-ttu-id="ac66d-321">особенно в том случае, если пользовательский интерфейс может предоставить достаточный объем работы, чтобы загрузить несколько фоновых задач.</span><span class="sxs-lookup"><span data-stu-id="ac66d-321">This is especially true if the UI can supply sufficient work to keep more than one background task busy.</span></span>

<span data-ttu-id="ac66d-322">Крайне важно обеспечить, чтобы фоновая задача могла быть автоматически перезапущена и имела достаточно ресурсов, чтобы по запросу справиться со всплесками нагрузки.</span><span class="sxs-lookup"><span data-stu-id="ac66d-322">It is vital to ensure that the background task can automatically restart and that it has sufficient capacity to cope with peaks in demand.</span></span> <span data-ttu-id="ac66d-323">Этого можно достигнуть, выделив вычислительный экземпляр с достаточными ресурсами, реализовав механизм очередей, который может сохранять запросы для последующего выполнения, когда потребность уменьшится, или совместив эти методы.</span><span class="sxs-lookup"><span data-stu-id="ac66d-323">You can achieve this by allocating a compute instance with sufficient resources, by implementing a queueing mechanism that can store requests for later execution when demand decreases, or by using a combination of these techniques.</span></span>

## <a name="coordination"></a><span data-ttu-id="ac66d-324">Координация</span><span class="sxs-lookup"><span data-stu-id="ac66d-324">Coordination</span></span>

<span data-ttu-id="ac66d-325">Фоновые задачи могут быть сложными и требовать выполнения нескольких отдельных задач для получения результата или для осуществления всех требований.</span><span class="sxs-lookup"><span data-stu-id="ac66d-325">The background tasks might be complex and might require multiple individual tasks to execute to produce a result or to fulfil all the requirements.</span></span> <span data-ttu-id="ac66d-326">Чаще всего в этих сценариях задача разделяется на более мелкие скрытые действия или подзадачи, которые могут быть выполнены несколькими объектами-получателями.</span><span class="sxs-lookup"><span data-stu-id="ac66d-326">It is common in these scenarios to divide the task into smaller discreet steps or subtasks that can be executed by multiple consumers.</span></span> <span data-ttu-id="ac66d-327">Многошаговые задания могут быть более эффективными и гибкими, так как отдельные шаги можно многократно использовать в нескольких заданиях.</span><span class="sxs-lookup"><span data-stu-id="ac66d-327">Multistep jobs can be more efficient and more flexible because individual steps might be reusable in multiple jobs.</span></span> <span data-ttu-id="ac66d-328">Также можно легко добавлять и удалять шаги или изменять их порядок.</span><span class="sxs-lookup"><span data-stu-id="ac66d-328">It is also easy to add, remove, or modify the order of the steps.</span></span>

<span data-ttu-id="ac66d-329">Координирование нескольких задач и шагов может оказаться сложной задачей, но существует три общих схемы, которые дозволено использовать для реализации решения:</span><span class="sxs-lookup"><span data-stu-id="ac66d-329">Coordinating multiple tasks and steps can be challenging, but there are three common patterns that you can use to guide your implementation of a solution:</span></span>

- <span data-ttu-id="ac66d-330">**Разложение задачи на несколько шагов, которые можно повторно использовать**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-330">**Decomposing a task into multiple reusable steps**.</span></span> <span data-ttu-id="ac66d-331">От приложения может потребоваться выполнение различных задач разной сложности с информацией, которую оно обрабатывает.</span><span class="sxs-lookup"><span data-stu-id="ac66d-331">An application might be required to perform a variety of tasks of varying complexity on the information that it processes.</span></span> <span data-ttu-id="ac66d-332">Для осуществления такой обработки в виде неделимого модуля может применяться простой, но гибкий способ реализации этого приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-332">A straightforward but inflexible approach to implementing this application might be to perform this processing as a monolithic module.</span></span> <span data-ttu-id="ac66d-333">Однако этот метод, вероятно, сократит возможности для рефакторинга кода, его оптимизации или повторного использования, если части одной и той же обработки потребуются в другом месте приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-333">However, this approach is likely to reduce the opportunities for refactoring the code, optimizing it, or reusing it if parts of the same processing are required elsewhere within the application.</span></span> <span data-ttu-id="ac66d-334">Дополнительные сведения см. в статье [Шаблон каналов и фильтров](../patterns/pipes-and-filters.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-334">For more information, see the [Pipes and Filters pattern](../patterns/pipes-and-filters.md).</span></span>

- <span data-ttu-id="ac66d-335">**Управление выполнением шагов для задачи**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-335">**Managing execution of the steps for a task**.</span></span> <span data-ttu-id="ac66d-336">Приложение может выполнять задачи, которые образуют ряд шагов, некоторые из которых в состоянии вызывать удаленные службы или обращаться к удаленным ресурсам.</span><span class="sxs-lookup"><span data-stu-id="ac66d-336">An application might perform tasks that comprise a number of steps (some of which might invoke remote services or access remote resources).</span></span> <span data-ttu-id="ac66d-337">Отдельные шаги могут быть независимы друг от друга, но они подчиняются логике приложения, которая реализует задачу.</span><span class="sxs-lookup"><span data-stu-id="ac66d-337">The individual steps might be independent of each other, but they are orchestrated by the application logic that implements the task.</span></span> <span data-ttu-id="ac66d-338">Дополнительные сведения см. в статье [Шаблон супервизора агента планировщика](../patterns/scheduler-agent-supervisor.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-338">For more information, see [Scheduler Agent Supervisor pattern](../patterns/scheduler-agent-supervisor.md).</span></span>

- <span data-ttu-id="ac66d-339">**Управление восстановлением для шагов задания, завершившихся сбоем**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-339">**Managing recovery for task steps that fail**.</span></span> <span data-ttu-id="ac66d-340">Приложению может потребоваться отменить работу, выполненную рядом шагов, которые вместе образуют согласованную операцию, если один или несколько шагов завершаются сбоем.</span><span class="sxs-lookup"><span data-stu-id="ac66d-340">An application might need to undo the work that is performed by a series of steps (which together define an eventually consistent operation) if one or more of the steps fail.</span></span> <span data-ttu-id="ac66d-341">Дополнительные сведения см. в статье [Шаблон компенсирующих транзакций](../patterns/compensating-transaction.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-341">For more information, see the [Compensating Transaction pattern](../patterns/compensating-transaction.md).</span></span>

## <a name="resiliency-considerations"></a><span data-ttu-id="ac66d-342">Рекомендации по обеспечению устойчивости</span><span class="sxs-lookup"><span data-stu-id="ac66d-342">Resiliency considerations</span></span>

<span data-ttu-id="ac66d-343">Фоновые задачи должны быть устойчивыми, чтобы предоставлять надежные службы для приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-343">Background tasks must be resilient in order to provide reliable services to the application.</span></span> <span data-ttu-id="ac66d-344">При планировании и проектировании фоновых задач необходимо учитывать следующее:</span><span class="sxs-lookup"><span data-stu-id="ac66d-344">When you are planning and designing background tasks, consider the following points:</span></span>

- <span data-ttu-id="ac66d-345">Фоновые задачи должны иметь возможность корректно обрабатывать перезапуск, не повреждая данные и не внося несогласованность в приложение.</span><span class="sxs-lookup"><span data-stu-id="ac66d-345">Background tasks must be able to gracefully handle restarts without corrupting data or introducing inconsistency into the application.</span></span> <span data-ttu-id="ac66d-346">Для длительных или многошаговых задач рассмотрите возможность использования *контрольных точек*: можно сохранять состояния заданий в постоянном хранилище или в виде сообщений в очереди, если это целесообразно.</span><span class="sxs-lookup"><span data-stu-id="ac66d-346">For long-running or multistep tasks, consider using *check pointing* by saving the state of jobs in persistent storage, or as messages in a queue if this is appropriate.</span></span> <span data-ttu-id="ac66d-347">Например, можно сохранить сведения о состоянии в сообщении в очереди и постепенно обновлять их по мере выполнения задачи, чтобы задачу можно было обработать с последней известной корректной контрольной точки, а не перезапускать с самого начала.</span><span class="sxs-lookup"><span data-stu-id="ac66d-347">For example, you can persist state information in a message in a queue and incrementally update this state information with the task progress so that the task can be processed from the last known good checkpoint--instead of restarting from the beginning.</span></span> <span data-ttu-id="ac66d-348">При использовании очередей служебной шины Azure этот сценарий реализуется с использованием сеансов обмена сообщениями.</span><span class="sxs-lookup"><span data-stu-id="ac66d-348">When using Azure Service Bus queues, you can use message sessions to enable the same scenario.</span></span> <span data-ttu-id="ac66d-349">Сеансы позволяют сохранять и извлекать состояние обработки приложения с помощью методов [SetState](/dotnet/api/microsoft.servicebus.messaging.messagesession.setstate?view=azureservicebus-4.0.0) и [GetState](/dotnet/api/microsoft.servicebus.messaging.messagesession.getstate?view=azureservicebus-4.0.0).</span><span class="sxs-lookup"><span data-stu-id="ac66d-349">Sessions allow you to save and retrieve the application processing state by using the [SetState](/dotnet/api/microsoft.servicebus.messaging.messagesession.setstate?view=azureservicebus-4.0.0) and [GetState](/dotnet/api/microsoft.servicebus.messaging.messagesession.getstate?view=azureservicebus-4.0.0) methods.</span></span> <span data-ttu-id="ac66d-350">Дополнительные сведения о проектировании надежных многошаговых процессов и рабочих процессов см. в статье [Шаблон супервизора агента планировщика](../patterns/scheduler-agent-supervisor.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-350">For more information about designing reliable multistep processes and workflows, see the [Scheduler Agent Supervisor pattern](../patterns/scheduler-agent-supervisor.md).</span></span>

- <span data-ttu-id="ac66d-351">Если для взаимодействия с фоновыми задачами используются очереди, они могут выполнять роль буфера для хранения запросов, отправленных к задачам, когда нагрузка на приложение выше, чем обычно.</span><span class="sxs-lookup"><span data-stu-id="ac66d-351">When you use queues to communicate with background tasks, the queues can act as a buffer to store requests that are sent to the tasks while the application is under higher than usual load.</span></span> <span data-ttu-id="ac66d-352">Это позволяет задачам перехватывать пользовательский интерфейс во время периодов снижения загруженности.</span><span class="sxs-lookup"><span data-stu-id="ac66d-352">This allows the tasks to catch up with the UI during less busy periods.</span></span> <span data-ttu-id="ac66d-353">Это также означает, что перезапуск не будет блокировать пользовательский интерфейс.</span><span class="sxs-lookup"><span data-stu-id="ac66d-353">It also means that restarts will not block the UI.</span></span> <span data-ttu-id="ac66d-354">Дополнительные сведения см. в статье [Шаблон балансировки нагрузки на основе очередей](../patterns/queue-based-load-leveling.md).</span><span class="sxs-lookup"><span data-stu-id="ac66d-354">For more information, see the [Queue-Based Load Leveling pattern](../patterns/queue-based-load-leveling.md).</span></span> <span data-ttu-id="ac66d-355">Если некоторые задачи важнее других, рассмотрите возможность реализации [шаблона очереди с приоритетом](../patterns/priority-queue.md), чтобы гарантировать первоочередное выполнение этих задач.</span><span class="sxs-lookup"><span data-stu-id="ac66d-355">If some tasks are more important than others, consider implementing the [Priority Queue pattern](../patterns/priority-queue.md) to ensure that these tasks run before less important ones.</span></span>

- <span data-ttu-id="ac66d-356">В фоновых задачах, которые инициируются сообщениями или обрабатывают их, необходимо реализовать обработку несогласованностей, например сообщений, поступающих не по порядку, сообщений, повторно вызывающих ошибку (часто их называют *сообщениями о сбое*) и сообщений, доставляемых более одного раза.</span><span class="sxs-lookup"><span data-stu-id="ac66d-356">Background tasks that are initiated by messages or process messages must be designed to handle inconsistencies, such as messages arriving out of order, messages that repeatedly cause an error (often referred to as *poison messages*), and messages that are delivered more than once.</span></span> <span data-ttu-id="ac66d-357">Рассмотрим следующее:</span><span class="sxs-lookup"><span data-stu-id="ac66d-357">Consider the following:</span></span>

  - <span data-ttu-id="ac66d-358">Сообщения, которые должны быть обработаны в определенном порядке, например те, что изменяют данные с учетом их существующего значения (например, добавляют значение к существующему значению), могут не поступить в первоначальном порядке, в котором были отправлены.</span><span class="sxs-lookup"><span data-stu-id="ac66d-358">Messages that must be processed in a specific order, such as those that change data based on the existing data value (for example, adding a value to an existing value), might not arrive in the original order in which they were sent.</span></span> <span data-ttu-id="ac66d-359">Кроме того, они могут быть обработаны разными экземплярами фоновой задачи в другом порядке из-за различной нагрузки каждого экземпляра.</span><span class="sxs-lookup"><span data-stu-id="ac66d-359">Alternatively, they might be handled by different instances of a background task in a different order due to varying loads on each instance.</span></span> <span data-ttu-id="ac66d-360">Сообщения, которые должны быть обработаны в определенном порядке, должны содержать порядковый номер, ключ или другой индикатор, который фоновые задачи могут использовать, чтобы убедиться, что они обрабатываются в правильном порядке.</span><span class="sxs-lookup"><span data-stu-id="ac66d-360">Messages that must be processed in a specific order should include a sequence number, key, or some other indicator that background tasks can use to ensure that they are processed in the correct order.</span></span> <span data-ttu-id="ac66d-361">При использовании служебной шины Azure, чтобы гарантировать порядок доставки, можно использовать сеансы обмена сообщениями.</span><span class="sxs-lookup"><span data-stu-id="ac66d-361">If you are using Azure Service Bus, you can use message sessions to guarantee the order of delivery.</span></span> <span data-ttu-id="ac66d-362">Однако обычно бывает целесообразнее спроектировать процесс таким образом, чтобы порядок сообщений не был важен.</span><span class="sxs-lookup"><span data-stu-id="ac66d-362">However, it is usually more efficient, where possible, to design the process so that the message order is not important.</span></span>

  - <span data-ttu-id="ac66d-363">Обычно фоновая задача считывает сообщения из очереди, которая временно скрывает их от других объектов-получателей сообщений</span><span class="sxs-lookup"><span data-stu-id="ac66d-363">Typically, a background task will peek at messages in the queue, which temporarily hides them from other message consumers.</span></span> <span data-ttu-id="ac66d-364">и удаляет после успешной обработки.</span><span class="sxs-lookup"><span data-stu-id="ac66d-364">Then it deletes the messages after they have been successfully processed.</span></span> <span data-ttu-id="ac66d-365">Если при обработке сообщения фоновой задачей произошел сбой, по истечении времени ожидания считывания оно снова появится в очереди</span><span class="sxs-lookup"><span data-stu-id="ac66d-365">If a background task fails when processing a message, that message will reappear on the queue after the peek time-out expires.</span></span> <span data-ttu-id="ac66d-366">и будет обработано другим экземпляром задачи или во время следующего цикла обработки данного экземпляра.</span><span class="sxs-lookup"><span data-stu-id="ac66d-366">It will be processed by another instance of the task or during the next processing cycle of this instance.</span></span> <span data-ttu-id="ac66d-367">Если сообщение постоянно приводит к ошибке в объекте-получателе, оно заблокирует задачу, очередь и, в конечном счете, само приложение, когда очередь заполнится.</span><span class="sxs-lookup"><span data-stu-id="ac66d-367">If the message consistently causes an error in the consumer, it will block the task, the queue, and eventually the application itself when the queue becomes full.</span></span> <span data-ttu-id="ac66d-368">Поэтому крайне важно выявлять и удалять из очереди сообщения о сбое.</span><span class="sxs-lookup"><span data-stu-id="ac66d-368">Therefore, it is vital to detect and remove poison messages from the queue.</span></span> <span data-ttu-id="ac66d-369">При использовании служебной шины Azure сообщения, вызвавшие ошибку, можно автоматически или вручную перемещать в связанную очередь недоставленных сообщений.</span><span class="sxs-lookup"><span data-stu-id="ac66d-369">If you are using Azure Service Bus, messages that cause an error can be moved automatically or manually to an associated dead letter queue.</span></span>

  - <span data-ttu-id="ac66d-370">Очереди регулируются механизмами доставки *хотя бы один раз*, но они могут доставлять одно и то же сообщение несколько раз.</span><span class="sxs-lookup"><span data-stu-id="ac66d-370">Queues are guaranteed at *least once* delivery mechanisms, but they might deliver the same message more than once.</span></span> <span data-ttu-id="ac66d-371">Кроме того, если сбой происходит после обработки сообщения фоновой задачей, но перед его удалением из очереди, сообщение станет доступным для повторной обработки.</span><span class="sxs-lookup"><span data-stu-id="ac66d-371">In addition, if a background task fails after processing a message but before deleting it from the queue, the message will become available for processing again.</span></span> <span data-ttu-id="ac66d-372">Фоновые задачи должны быть идемпотентными. Это означает, что повторная обработка одного и того же сообщения не вызывает ошибку или несогласованность в данных приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-372">Background tasks should be idempotent, which means that processing the same message more than once does not cause an error or inconsistency in the application’s data.</span></span> <span data-ttu-id="ac66d-373">Некоторые операции идемпотентны по своему характеру. Например, задание определенного нового значения для сохраненного значения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-373">Some operations are naturally idempotent, such as setting a stored value to a specific new value.</span></span> <span data-ttu-id="ac66d-374">Однако такие операции, как добавление значения к существующему хранимому значению без проверки, осталось ли сохраненное значение таким же, как при изначальной отправке сообщения, приведут к несогласованности.</span><span class="sxs-lookup"><span data-stu-id="ac66d-374">However, operations such as adding a value to an existing stored value without checking that the stored value is still the same as when the message was originally sent will cause inconsistencies.</span></span> <span data-ttu-id="ac66d-375">Очереди служебной шины Azure можно настроить для автоматического удаления повторяющихся сообщений.</span><span class="sxs-lookup"><span data-stu-id="ac66d-375">Azure Service Bus queues can be configured to automatically remove duplicated messages.</span></span>

  - <span data-ttu-id="ac66d-376">Некоторые системы обмена сообщениями, например очереди хранилища Azure и очереди служебной шины Azure, поддерживают свойство количества выводов из очереди, указывающее, сколько раз сообщение было считано из очереди.</span><span class="sxs-lookup"><span data-stu-id="ac66d-376">Some messaging systems, such as Azure storage queues and Azure Service Bus queues, support a de-queue count property that indicates the number of times a message has been read from the queue.</span></span> <span data-ttu-id="ac66d-377">Это может быть удобно для обработки повторяющихся сообщений и сообщений о сбое.</span><span class="sxs-lookup"><span data-stu-id="ac66d-377">This can be useful in handling repeated and poison messages.</span></span> <span data-ttu-id="ac66d-378">Дополнительную информацию см. в разделах [Учебник по асинхронному обмену сообщениями](https://msdn.microsoft.com/library/dn589781.aspx) и [Шаблоны идемпотентности](https://blog.jonathanoliver.com/idempotency-patterns/).</span><span class="sxs-lookup"><span data-stu-id="ac66d-378">For more information, see [Asynchronous Messaging Primer](https://msdn.microsoft.com/library/dn589781.aspx) and [Idempotency Patterns](https://blog.jonathanoliver.com/idempotency-patterns/).</span></span>

## <a name="scaling-and-performance-considerations"></a><span data-ttu-id="ac66d-379">Рекомендации по производительности и масштабированию</span><span class="sxs-lookup"><span data-stu-id="ac66d-379">Scaling and performance considerations</span></span>

<span data-ttu-id="ac66d-380">Фоновые задачи должны обеспечивать достаточную производительность, чтобы не блокировать работу приложения и не приводить к несогласованности из-за отложенных операций, когда система находится под нагрузкой.</span><span class="sxs-lookup"><span data-stu-id="ac66d-380">Background tasks must offer sufficient performance to ensure they do not block the application, or cause inconsistencies due to delayed operation when the system is under load.</span></span> <span data-ttu-id="ac66d-381">Как правило, производительность повышается за счет масштабирования вычислительных экземпляров, в которых размещены фоновые задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-381">Typically, performance is improved by scaling the compute instances that host the background tasks.</span></span> <span data-ttu-id="ac66d-382">При планировании и проектировании фоновых задач необходимо учитывать следующие моменты, связанные с масштабируемостью и производительностью:</span><span class="sxs-lookup"><span data-stu-id="ac66d-382">When you are planning and designing background tasks, consider the following points around scalability and performance:</span></span>

- <span data-ttu-id="ac66d-383">Azure поддерживает автоматическое масштабирование (горизонтальное) на основе текущих запросов и загрузки или на основе предопределенного расписания для веб-приложений, а также развертываний, размещенных на виртуальных машинах.</span><span class="sxs-lookup"><span data-stu-id="ac66d-383">Azure supports autoscaling (both scaling out and scaling back in) based on current demand and load or on a predefined schedule, for Web Apps and Virtual Machines hosted deployments.</span></span> <span data-ttu-id="ac66d-384">Эту функцию можно использовать, чтобы обеспечить приложение в целом достаточными возможностями производительности, минимизируя затрачиваемое время выполнения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-384">Use this feature to ensure that the application as a whole has sufficient performance capabilities while minimizing runtime costs.</span></span>

- <span data-ttu-id="ac66d-385">Когда у фоновых задач иные возможности производительности, чем у других частей приложения (например пользовательского интерфейса или таких компонентов, как уровень доступа к данным), размещение фоновых задач вместе в отдельной службе вычислений позволяет независимо масштабировать пользовательский интерфейс и фоновые задачи, чтобы управлять нагрузкой.</span><span class="sxs-lookup"><span data-stu-id="ac66d-385">Where background tasks have a different performance capability from the other parts of a application (for example, the UI or components such as the data access layer), hosting the background tasks together in a separate compute service allows the UI and background tasks to scale independently to manage the load.</span></span> <span data-ttu-id="ac66d-386">Если возможности производительности нескольких фоновых задач значительно отличаются друг от друга, рекомендуется разделить их и масштабировать независимо.</span><span class="sxs-lookup"><span data-stu-id="ac66d-386">If multiple background tasks have significantly different performance capabilities from each other, consider dividing them and scaling each type independently.</span></span> <span data-ttu-id="ac66d-387">Но имейте в виду, что это может увеличить расходы.</span><span class="sxs-lookup"><span data-stu-id="ac66d-387">However, note that this might increase runtime costs.</span></span>

- <span data-ttu-id="ac66d-388">Простого масштабирования вычислительных ресурсов может оказаться недостаточно, чтобы предотвратить потерю производительности под нагрузкой.</span><span class="sxs-lookup"><span data-stu-id="ac66d-388">Simply scaling the compute resources might not be sufficient to prevent loss of performance under load.</span></span> <span data-ttu-id="ac66d-389">Также может потребоваться масштабировать очереди хранилища и другие ресурсы, чтобы не дать одной точке в общей цепочке обработки стать узким местом.</span><span class="sxs-lookup"><span data-stu-id="ac66d-389">You might also need to scale storage queues and other resources to prevent a single point of the overall processing chain from becoming a bottleneck.</span></span> <span data-ttu-id="ac66d-390">Также рассмотрите другие ограничения, например максимальную пропускную способность хранилища и других служб, от которых зависит приложение и фоновые задачи.</span><span class="sxs-lookup"><span data-stu-id="ac66d-390">Also, consider other limitations, such as the maximum throughput of storage and other services that the application and the background tasks rely on.</span></span>

- <span data-ttu-id="ac66d-391">Фоновые задачи должен быть предназначены для масштабирования.</span><span class="sxs-lookup"><span data-stu-id="ac66d-391">Background tasks must be designed for scaling.</span></span> <span data-ttu-id="ac66d-392">Например, они должны иметь возможность динамически определять количество используемых очередей хранилища, чтобы прослушивать или отправлять сообщения в соответствующую очередь.</span><span class="sxs-lookup"><span data-stu-id="ac66d-392">For example, they must be able to dynamically detect the number of storage queues in use in order to listen on or send messages to the appropriate queue.</span></span>

- <span data-ttu-id="ac66d-393">По умолчанию веб-задания масштабируются вместе с соответствующим экземпляром веб-приложений Azure.</span><span class="sxs-lookup"><span data-stu-id="ac66d-393">By default, WebJobs scale with their associated Azure Web Apps instance.</span></span> <span data-ttu-id="ac66d-394">Тем не менее, если требуется, чтобы веб-задание выполнялось только в одном экземпляре, можно создать файл Settings.job, содержащий данные JSON **{ "is_singleton": true }**.</span><span class="sxs-lookup"><span data-stu-id="ac66d-394">However, if you want a WebJob to run as only a single instance, you can create a Settings.job file that contains the JSON data **{ "is_singleton": true }**.</span></span> <span data-ttu-id="ac66d-395">Это заставит Azure запускать только один экземпляр веб-задания, даже если имеется несколько экземпляров связанного веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="ac66d-395">This forces Azure to only run one instance of the WebJob, even if there are multiple instances of the associated web app.</span></span> <span data-ttu-id="ac66d-396">Этот метод удобно использовать для запланированных заданий, которые должны выполняться только в одном экземпляре.</span><span class="sxs-lookup"><span data-stu-id="ac66d-396">This can be a useful technique for scheduled jobs that must run as only a single instance.</span></span>

## <a name="related-patterns"></a><span data-ttu-id="ac66d-397">Связанные шаблоны</span><span class="sxs-lookup"><span data-stu-id="ac66d-397">Related patterns</span></span>

- [<span data-ttu-id="ac66d-398">Рекомендации по разделению вычислений</span><span class="sxs-lookup"><span data-stu-id="ac66d-398">Compute Partitioning Guidance</span></span>](https://msdn.microsoft.com/library/dn589773.aspx)
