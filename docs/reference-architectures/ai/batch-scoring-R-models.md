---
title: Пакетная оценка с моделями R в Azure
description: Выполнение пакетной оценки при помощи R-модели, с помощью пакетной службы Azure и набор данных по прогнозированию продаж магазина розничной торговли.
author: njray
ms.date: 03/29/2019
ms.topic: reference-architecture
ms.service: architecture-center
ms.subservice: reference-architecture
ms.custom: azcat-ai
ms.openlocfilehash: 4fa57168c337b01c8e7d0fc86ba54fee59a7ae47
ms.sourcegitcommit: 1a3cc91530d56731029ea091db1f15d41ac056af
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/03/2019
ms.locfileid: "58888068"
---
# <a name="batch-scoring-of-r-machine-learning-models-on-azure"></a><span data-ttu-id="5813d-103">Пакетная оценка моделей машинного обучения R в Azure</span><span class="sxs-lookup"><span data-stu-id="5813d-103">Batch scoring of R machine learning models on Azure</span></span>

<span data-ttu-id="5813d-104">На схеме эталонной архитектуры показано выполнение пакетной оценки с помощью моделей R с помощью пакетной службы Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-104">This reference architecture shows how to perform batch scoring with R models using Azure Batch.</span></span> <span data-ttu-id="5813d-105">Сценарий основан на прогнозированию продаж магазина розничной торговли, но эту архитектуру можно обобщить для любого сценария, необходимости создания прогнозов на больших scaler, с помощью R-модели.</span><span class="sxs-lookup"><span data-stu-id="5813d-105">The scenario is based on retail store sales forecasting, but this architecture can be generalized for any scenario requiring the generation of predictions on a large scaler using R models.</span></span> <span data-ttu-id="5813d-106">Эталонную реализацию для этой архитектуры можно найти на сайте [GitHub][github].</span><span class="sxs-lookup"><span data-stu-id="5813d-106">A reference implementation for this architecture is available on [GitHub][github].</span></span>

![Схема архитектуры][0]

<span data-ttu-id="5813d-108">**Сценарий**. Супермаркетов должен для прогнозирования продаж продуктов предстоящих квартала.</span><span class="sxs-lookup"><span data-stu-id="5813d-108">**Scenario**: A supermarket chain needs to forecast sales of products over the upcoming quarter.</span></span> <span data-ttu-id="5813d-109">Прогноз позволяет компании лучше управлять своей цепочке поставок и убедитесь, что сможет удовлетворить спрос на продукты в каждом из магазинов.</span><span class="sxs-lookup"><span data-stu-id="5813d-109">The forecast allows the company to manage its supply chain better and ensure it can meet demand for products at each of its stores.</span></span> <span data-ttu-id="5813d-110">Компании обновляет ее прогнозируемые каждую неделю по мере поступления новых данных о продажах из предыдущей недели и маркетинговые стратегии для следующего квартала продукта имеет значение.</span><span class="sxs-lookup"><span data-stu-id="5813d-110">The company updates its forecasts every week as new sales data from the previous week becomes available and the product marketing strategy for next quarter is set.</span></span> <span data-ttu-id="5813d-111">Квантильная прогнозы создаются для оценки неопределенность отдельные прогнозы продаж.</span><span class="sxs-lookup"><span data-stu-id="5813d-111">Quantile forecasts are generated to estimate the uncertainty of the individual sales forecasts.</span></span>

<span data-ttu-id="5813d-112">Обработка предусматривает указанные ниже действия.</span><span class="sxs-lookup"><span data-stu-id="5813d-112">Processing involves the following steps:</span></span>

1. <span data-ttu-id="5813d-113">Приложения логики Azure активирует в процессе создания прогноза, один раз в неделю.</span><span class="sxs-lookup"><span data-stu-id="5813d-113">An Azure Logic App triggers the forecast generation process once per week.</span></span>

1. <span data-ttu-id="5813d-114">Приложение логики запускается экземпляр контейнера Azure, контейнер Docker, который активирует оценки заданий в кластере пакета планировщик.</span><span class="sxs-lookup"><span data-stu-id="5813d-114">The logic app starts an Azure Container Instance running the scheduler Docker container, which triggers the scoring jobs on the Batch cluster.</span></span>

1. <span data-ttu-id="5813d-115">Оценки задания выполняются в параллельном режиме на узлах кластера пакетной службы.</span><span class="sxs-lookup"><span data-stu-id="5813d-115">Scoring jobs run in parallel across the nodes of the Batch cluster.</span></span> <span data-ttu-id="5813d-116">Каждый узел:</span><span class="sxs-lookup"><span data-stu-id="5813d-116">Each node:</span></span>

    1. <span data-ttu-id="5813d-117">Извлекает рабочую роль образ Docker из Docker Hub и запускает контейнер.</span><span class="sxs-lookup"><span data-stu-id="5813d-117">Pulls the worker Docker image from Docker Hub and starts a container.</span></span>

    1. <span data-ttu-id="5813d-118">Считывает входные данные и предварительно обученных моделей R из хранилища BLOB-объектов Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-118">Reads input data and pre-trained R models from Azure Blob storage.</span></span>

    1. <span data-ttu-id="5813d-119">Оценивает данные для создания прогнозов.</span><span class="sxs-lookup"><span data-stu-id="5813d-119">Scores the data to produce the forecasts.</span></span>

    1. <span data-ttu-id="5813d-120">Записывает результаты прогноза в хранилище BLOB-объектов.</span><span class="sxs-lookup"><span data-stu-id="5813d-120">Writes the forecast results to blob storage.</span></span>

<span data-ttu-id="5813d-121">Ниже показаны прогнозируемые продажи для четырех продуктов (номера SKU) в одном хранилище.</span><span class="sxs-lookup"><span data-stu-id="5813d-121">The figure below shows the forecasted sales for four products (SKUs) in one store.</span></span> <span data-ttu-id="5813d-122">Черная линия — это журнал продаж, пунктирная линия является Медиана (q50) прогноза, розовый аппаратного контроллера управления представляет процентили двадцать пятой и пятой семьдесят, а синяя полоса процентили пятая и девяносто пятой.</span><span class="sxs-lookup"><span data-stu-id="5813d-122">The black line is the sales history, the dashed line is the median (q50) forecast, the pink band represents the twenty-fifth and seventy-fifth percentiles, and the blue band represents the fifth and ninety-fifth percentiles.</span></span>

![Прогнозы продаж][1]

## <a name="architecture"></a><span data-ttu-id="5813d-124">Архитектура</span><span class="sxs-lookup"><span data-stu-id="5813d-124">Architecture</span></span>

<span data-ttu-id="5813d-125">Архитектура состоит из следующих компонентов.</span><span class="sxs-lookup"><span data-stu-id="5813d-125">This architecture consists of the following components.</span></span>

<span data-ttu-id="5813d-126">[Пакетная служба Azure] [ batch] используется для запуска задания создания прогноза в параллельном режиме в кластере виртуальных машин.</span><span class="sxs-lookup"><span data-stu-id="5813d-126">[Azure Batch][batch] is used to run forecast generation jobs in parallel on a cluster of virtual machines.</span></span> <span data-ttu-id="5813d-127">Прогнозы выполняются с помощью предварительно обученную машинного обучения моделей, реализован в пакетной службе Azure R. можно автоматически масштабировать число виртуальных машин на основе числа заданий, отправленных в кластер.</span><span class="sxs-lookup"><span data-stu-id="5813d-127">Predictions are made using pre-trained machine learning models implemented in R. Azure Batch can automatically scale the number of VMs based on the number of jobs submitted to the cluster.</span></span> <span data-ttu-id="5813d-128">На каждом узле скрипт R выполняется в контейнере Docker для оценки данных и создания прогнозов.</span><span class="sxs-lookup"><span data-stu-id="5813d-128">On each node, an R script runs within a Docker container to score data and generate forecasts.</span></span>

<span data-ttu-id="5813d-129">[Хранилище BLOB-объектов] [ blob] используется для хранения входных данных, моделей предварительно обученной машинного обучения и прогнозирования результатов.</span><span class="sxs-lookup"><span data-stu-id="5813d-129">[Azure Blob Storage][blob] is used to store the input data, the pre-trained machine learning models, and the forecast results.</span></span> <span data-ttu-id="5813d-130">Он обеспечивает очень экономичное хранилище за производительность, которая требуется для этой рабочей нагрузки.</span><span class="sxs-lookup"><span data-stu-id="5813d-130">It delivers very cost-effective storage for the performance that this workload requires.</span></span>

<span data-ttu-id="5813d-131">[Экземпляры контейнеров Azure] [ aci] предоставляют беcсерверные вычисления по требованию.</span><span class="sxs-lookup"><span data-stu-id="5813d-131">[Azure Container Instances][aci] provide serverless compute on demand.</span></span> <span data-ttu-id="5813d-132">В этом случае экземпляр контейнера развертывается в соответствии с расписанием запуска пакетных заданий, которые создают прогнозы.</span><span class="sxs-lookup"><span data-stu-id="5813d-132">In this case, a container instance is deployed on a schedule to trigger the Batch jobs that generate the forecasts.</span></span> <span data-ttu-id="5813d-133">Пакетные задания запускаются из сценария R с помощью [doAzureParallel][doAzureParallel] пакета.</span><span class="sxs-lookup"><span data-stu-id="5813d-133">The Batch jobs are triggered from an R script using the [doAzureParallel][doAzureParallel] package.</span></span> <span data-ttu-id="5813d-134">Экземпляр контейнера автоматически завершает работу после окончания задания.</span><span class="sxs-lookup"><span data-stu-id="5813d-134">The container instance automatically shuts down once the jobs have finished.</span></span>

<span data-ttu-id="5813d-135">[Служба Azure Logic Apps] [ logic-apps] весь рабочий процесс запускается путем развертывания экземпляров контейнера по расписанию.</span><span class="sxs-lookup"><span data-stu-id="5813d-135">[Azure Logic Apps][logic-apps] trigger the entire workflow by deploying the container instances on a schedule.</span></span> <span data-ttu-id="5813d-136">Соединитель экземпляров контейнеров Azure в приложениях логики позволяет экземпляру развертывается от диапазона событий триггера.</span><span class="sxs-lookup"><span data-stu-id="5813d-136">An Azure Container Instances connector in Logic Apps allows an instance to be deployed upon a range of trigger events.</span></span>

## <a name="performance-considerations"></a><span data-ttu-id="5813d-137">Рекомендации по производительности</span><span class="sxs-lookup"><span data-stu-id="5813d-137">Performance considerations</span></span>

### <a name="containerized-deployment"></a><span data-ttu-id="5813d-138">Контейнерное развертывание</span><span class="sxs-lookup"><span data-stu-id="5813d-138">Containerized deployment</span></span>

<span data-ttu-id="5813d-139">Благодаря такой архитектуре попадают все скрипты R [Docker](https://www.docker.com/) контейнеры.</span><span class="sxs-lookup"><span data-stu-id="5813d-139">With this architecture, all R scripts run within [Docker](https://www.docker.com/) containers.</span></span> <span data-ttu-id="5813d-140">Это гарантирует, что сценарии запускать в согласованной среде, с той же версией R и версии пакетов, каждый раз.</span><span class="sxs-lookup"><span data-stu-id="5813d-140">This ensures that the scripts run in a consistent environment, with the same R version and packages versions, every time.</span></span> <span data-ttu-id="5813d-141">Отдельные образы Docker используются для контейнеров, планировщик и рабочей роли, так как каждый имеет другой набор зависимостей пакетов R.</span><span class="sxs-lookup"><span data-stu-id="5813d-141">Separate Docker images are used for the scheduler and worker containers, because each has a different set of R package dependencies.</span></span>

<span data-ttu-id="5813d-142">Экземпляры контейнеров Azure предоставляют бессерверной среде для запуска контейнера планировщика.</span><span class="sxs-lookup"><span data-stu-id="5813d-142">Azure Container Instances provides a serverless environment to run the scheduler container.</span></span> <span data-ttu-id="5813d-143">Контейнер планировщик выполняет сценарий R, запускает отдельные оценки заданий, запущенных на кластере пакетной службы Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-143">The scheduler container runs an R script that triggers the individual scoring jobs running on an Azure Batch cluster.</span></span>

<span data-ttu-id="5813d-144">На каждом узле кластера пакета выполняется контейнера рабочей роли, которые выполняет скрипт оценки.</span><span class="sxs-lookup"><span data-stu-id="5813d-144">Each node of the Batch cluster runs the worker container, which executes the scoring script.</span></span>

### <a name="parallelizing-the-workload"></a><span data-ttu-id="5813d-145">Параллелизация рабочей нагрузки</span><span class="sxs-lookup"><span data-stu-id="5813d-145">Parallelizing the workload</span></span>

<span data-ttu-id="5813d-146">При пакетной оценки данных с помощью модели R, рассмотрите возможность параллельного выполнения рабочей нагрузки.</span><span class="sxs-lookup"><span data-stu-id="5813d-146">When batch scoring data with R models, consider how to parallelize the workload.</span></span> <span data-ttu-id="5813d-147">Входные данные должны быть секционированы каким-либо образом, чтобы операция оценки можно распространять на узлах кластера.</span><span class="sxs-lookup"><span data-stu-id="5813d-147">The input data must be partitioned somehow so that the scoring operation can be distributed  across the cluster nodes.</span></span> <span data-ttu-id="5813d-148">Попробуйте разные способы обнаружения лучшим выбором для распределения рабочей нагрузки.</span><span class="sxs-lookup"><span data-stu-id="5813d-148">Try different approaches to discover the best choice for distributing your workload.</span></span> <span data-ttu-id="5813d-149">Для каждого конкретного случая необходимо учитывайте следующее:</span><span class="sxs-lookup"><span data-stu-id="5813d-149">On a case-by-case basis, consider the following:</span></span>

- <span data-ttu-id="5813d-150">Какой объем данных может быть загружаются и обрабатываются в память из одного узла.</span><span class="sxs-lookup"><span data-stu-id="5813d-150">How much data can be loaded and processed in the memory of a single node.</span></span>
- <span data-ttu-id="5813d-151">Затраты на запуск пакетных заданий.</span><span class="sxs-lookup"><span data-stu-id="5813d-151">The overhead of starting each batch job.</span></span>
- <span data-ttu-id="5813d-152">Загрузка моделей R издержки.</span><span class="sxs-lookup"><span data-stu-id="5813d-152">The overhead of loading the R models.</span></span>

<span data-ttu-id="5813d-153">В сценарий, используемый в этом примере объекты моделей являются большие и занимает всего несколько секунд, чтобы создать прогноз для отдельных продуктов.</span><span class="sxs-lookup"><span data-stu-id="5813d-153">In the scenario used for this example, the model objects are large, and it takes only a few seconds to generate a forecast for individual products.</span></span> <span data-ttu-id="5813d-154">По этой причине можно сгруппировать продукты и выполнения одного задания пакетной службы на каждом узле.</span><span class="sxs-lookup"><span data-stu-id="5813d-154">For this reason, you can group the products and execute a single Batch job per node.</span></span> <span data-ttu-id="5813d-155">Цикл в каждое задание создает прогнозы для продуктов последовательно.</span><span class="sxs-lookup"><span data-stu-id="5813d-155">A loop within each job generates forecasts for the products sequentially.</span></span> <span data-ttu-id="5813d-156">Этот метод оказывается наиболее эффективный способ для параллельного выполнения этой конкретной рабочей нагрузки.</span><span class="sxs-lookup"><span data-stu-id="5813d-156">This method turns out to be the most efficient way to parallelize this particular workload.</span></span> <span data-ttu-id="5813d-157">Это позволяет избежать нагрузки для запуска многих небольших пакетных заданий и многократно Загрузка моделей R.</span><span class="sxs-lookup"><span data-stu-id="5813d-157">It avoids the overhead of starting many smaller Batch jobs and repeatedly loading the R models.</span></span>

<span data-ttu-id="5813d-158">Альтернативный подход — для запуска одного пакетного задания на продукт.</span><span class="sxs-lookup"><span data-stu-id="5813d-158">An alternative approach is to trigger one Batch job per product.</span></span> <span data-ttu-id="5813d-159">Пакетная служба Azure автоматически формирует очередь заданий и отправляет их выполнение в кластере как узлы становятся доступными.</span><span class="sxs-lookup"><span data-stu-id="5813d-159">Azure Batch automatically forms a queue of jobs and submits them to be executed on the cluster as nodes become available.</span></span> <span data-ttu-id="5813d-160">Используйте [автоматическое масштабирование] [ autoscale] корректировать количество узлов в кластере, в зависимости от количества заданий.</span><span class="sxs-lookup"><span data-stu-id="5813d-160">Use [automatic scaling][autoscale] to adjust the number of nodes in the cluster depending on the number of jobs.</span></span> <span data-ttu-id="5813d-161">Этот подход более рационален Если занимает достаточно долго для завершения каждой операции оценки, выравнивание нагрузки для запуска задания и повторной загрузки объекты модели.</span><span class="sxs-lookup"><span data-stu-id="5813d-161">This approach makes more sense if it takes a relatively long time to complete each scoring operation, justifying the overhead of starting the jobs and reloading the model objects.</span></span> <span data-ttu-id="5813d-162">Этот подход также проще реализовать и дает возможность использовать автоматическое масштабирование, важно учитывать, если размер рабочая нагрузка не известны заранее.</span><span class="sxs-lookup"><span data-stu-id="5813d-162">This approach is also simpler to implement and gives you the flexibility to use automatic scaling—an important consideration if the size of the total workload is not known in advance.</span></span>

## <a name="monitoring-and-logging-considerations"></a><span data-ttu-id="5813d-163">Мониторинг и ведение журнала запросов</span><span class="sxs-lookup"><span data-stu-id="5813d-163">Monitoring and logging considerations</span></span>

### <a name="monitoring-azure-batch-jobs"></a><span data-ttu-id="5813d-164">Отслеживание заданий пакетной службы Azure</span><span class="sxs-lookup"><span data-stu-id="5813d-164">Monitoring Azure Batch jobs</span></span>

<span data-ttu-id="5813d-165">Мониторинг и завершить пакетных заданий из **заданий** области учетной записи пакетной службы на портале Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-165">Monitor and terminate Batch jobs from the **Jobs** pane of the Batch account in the Azure portal.</span></span> <span data-ttu-id="5813d-166">Мониторинг кластера пакетной службы, состояние отдельных узлов, в том числе из **пулы** области.</span><span class="sxs-lookup"><span data-stu-id="5813d-166">Monitor the batch cluster, including the state of individual nodes, from the **Pools** pane.</span></span>

### <a name="logging-with-doazureparallel"></a><span data-ttu-id="5813d-167">Ведение журнала с помощью doAzureParallel</span><span class="sxs-lookup"><span data-stu-id="5813d-167">Logging with doAzureParallel</span></span>

<span data-ttu-id="5813d-168">Пакет doAzureParallel автоматически собирает журналы из всех stdout и stderr для каждого задания, отправленные в пакетной службе Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-168">The doAzureParallel package automatically collects logs of all stdout/stderr for every job submitted on Azure Batch.</span></span> <span data-ttu-id="5813d-169">Их можно найти в учетной записи хранения, которая создается при установке.</span><span class="sxs-lookup"><span data-stu-id="5813d-169">These can be found in the storage account created at setup.</span></span> <span data-ttu-id="5813d-170">Чтобы просмотреть их, используйте средства навигации хранилища, такие как [обозреватель службы хранилища Azure] [ storage-explorer] или на портале Azure.</span><span class="sxs-lookup"><span data-stu-id="5813d-170">To view them, use a storage navigation tool such as [Azure Storage Explorer][storage-explorer] or Azure portal.</span></span>

<span data-ttu-id="5813d-171">Чтобы быстро отладить пакетных заданий во время разработки, печать журналы в локальный сеанс R с помощью [getJobFiles][getJobFiles] функции doAzureParallel.</span><span class="sxs-lookup"><span data-stu-id="5813d-171">To quickly debug Batch jobs during development, print logs in your local R session using the [getJobFiles][getJobFiles] function of doAzureParallel.</span></span>

## <a name="cost-considerations"></a><span data-ttu-id="5813d-172">Рекомендации по стоимости</span><span class="sxs-lookup"><span data-stu-id="5813d-172">Cost considerations</span></span>

<span data-ttu-id="5813d-173">Вычислительные ресурсы, используемые в этой эталонной архитектуре — это вызывающих наибольшие издержки компоненты.</span><span class="sxs-lookup"><span data-stu-id="5813d-173">The compute resources used in this reference architecture are the most costly components.</span></span> <span data-ttu-id="5813d-174">В этом сценарии кластер фиксированного размера создается каждый раз, когда задание запускается, а затем завершите работу после завершения задания.</span><span class="sxs-lookup"><span data-stu-id="5813d-174">For this scenario, a cluster of fixed size is created whenever the job is triggered and then shut down after the job has completed.</span></span> <span data-ttu-id="5813d-175">Плата взимается только при запуске, запуск или завершении работы узлов кластера.</span><span class="sxs-lookup"><span data-stu-id="5813d-175">Cost is incurred only while the cluster nodes are starting, running, or shutting down.</span></span> <span data-ttu-id="5813d-176">Этот подход используется в сценарии, где вычислительные ресурсы, необходимые для создания прогнозов не изменяются из задания.</span><span class="sxs-lookup"><span data-stu-id="5813d-176">This approach is suitable for a scenario where the compute resources required to generate the forecasts remain relatively constant from job to job.</span></span>

<span data-ttu-id="5813d-177">В сценариях, когда объем вычислений, необходимых для выполнения задания не известен заранее возможно, более подходящий для использования автоматического масштабирования.</span><span class="sxs-lookup"><span data-stu-id="5813d-177">In scenarios where the amount of compute required to complete the job is not known in advance, it may be more suitable to use automatic scaling.</span></span> <span data-ttu-id="5813d-178">При таком подходе размер кластера масштабируется вверх или вниз в зависимости от размера задания.</span><span class="sxs-lookup"><span data-stu-id="5813d-178">With this approach, the size of the cluster is scaled up or down depending on the size of the job.</span></span> <span data-ttu-id="5813d-179">Пакетная служба Azure поддерживает широкий спектр формулы автоматического масштабирования, которые можно задать при определении кластера с помощью [doAzureParallel][doAzureParallel] API.</span><span class="sxs-lookup"><span data-stu-id="5813d-179">Azure Batch supports a range of auto-scale formulae which you can set when defining the cluster using the [doAzureParallel][doAzureParallel] API.</span></span>

<span data-ttu-id="5813d-180">В некоторых случаях время между заданиями, может быть слишком коротким для запуска кластера и завершить работу.</span><span class="sxs-lookup"><span data-stu-id="5813d-180">For some scenarios, the time between jobs may be too short to shut down and start up the cluster.</span></span> <span data-ttu-id="5813d-181">В таких случаях все кластере работает между заданиями, при необходимости.</span><span class="sxs-lookup"><span data-stu-id="5813d-181">In these cases, keep the cluster running between jobs if appropriate.</span></span>

<span data-ttu-id="5813d-182">Azure пакетной службы и doAzureParallel поддерживают использование низкоприоритетных виртуальных машин.</span><span class="sxs-lookup"><span data-stu-id="5813d-182">Azure Batch and doAzureParallel support the use of low-priority VMs.</span></span> <span data-ttu-id="5813d-183">Эти виртуальные машины в состав более значительную скидку, но риск appropriated с других рабочих нагрузок с высоким приоритетом.</span><span class="sxs-lookup"><span data-stu-id="5813d-183">These VMs come with a significant discount but risk being appropriated by other higher priority workloads.</span></span> <span data-ttu-id="5813d-184">Поэтому использование этих виртуальных машин не рекомендуются для критических рабочих нагрузок.</span><span class="sxs-lookup"><span data-stu-id="5813d-184">The use of these VMs are therefore not recommended for critical production workloads.</span></span> <span data-ttu-id="5813d-185">Тем не менее, они очень полезны для экспериментальных или рабочих нагрузок при разработке.</span><span class="sxs-lookup"><span data-stu-id="5813d-185">However, they are very useful for experimental or development workloads.</span></span>

## <a name="deployment"></a><span data-ttu-id="5813d-186">Развертывание</span><span class="sxs-lookup"><span data-stu-id="5813d-186">Deployment</span></span>

<span data-ttu-id="5813d-187">Для развертывания этой эталонной архитектуры, выполните действия, описанные в [GitHub][github] репозитория.</span><span class="sxs-lookup"><span data-stu-id="5813d-187">To deploy this reference architecture, follow the steps described in the [GitHub][github] repo.</span></span>


[0]: ./_images/batch-scoring-r-models.png
[1]: ./_images/sales-forecasts.png
[aci]: /azure/container-instances/container-instances-overview
[autoscale]: /azure/batch/batch-automatic-scaling
[batch]: /azure/batch/batch-technical-overview
[blob]: /azure/storage/blobs/storage-blobs-introduction
[doAzureParallel]: https://github.com/Azure/doAzureParallel/blob/master/docs/32-autoscale.md
[getJobFiles]: /azure/machine-learning/service/how-to-train-ml-models
[github]: https://github.com/Azure/RBatchScoring
[logic-apps]: /azure/logic-apps/logic-apps-overview
[storage-explorer]: /azure/vs-azure-tools-storage-manage-with-storage-explorer?tabs=windows